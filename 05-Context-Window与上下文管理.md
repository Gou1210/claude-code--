# 5. Context Window 与上下文管理

## 重要程度
**S 级** - 必须掌握。不理解上下文管理，Claude Code 很容易越用越“重”，最后变慢、变乱、变不稳定。

## 学习目标
- 理解 Claude Code 为什么会在长会话里逐渐变钝
- 知道哪些信息会自动进入上下文，哪些不会
- 学会在不丢失关键约束的前提下控制上下文体积
- 能判断什么时候该继续当前 session，什么时候该 compact，什么时候该新开会话

## 先纠正一个常见误区
很多人把“上下文窗口”理解成“聊天记录长度”。

这不准确。

在 Claude Code 里，上下文不只是你和 Claude 的对话，还包括：
- 当前会话里的消息历史
- 读过的文件内容
- 命令输出
- 启动时加载的 `CLAUDE.md`
- auto memory
- skill 描述与被调用后的 skill 正文
- MCP 工具名称，以及按需展开的工具定义
- 系统提示与部分运行时元信息

所以你会感觉“我也没聊多少，怎么就变重了”，本质上往往不是聊天太多，而是读了太多文件、贴了太多日志、拉进了太多无关上下文。

## 学什么
- context window 是什么
- Claude Code 的上下文是怎么一步步涨起来的
- `/context`、`/compact`、`/clear` 的作用边界
- `claude --continue`、`claude --resume` 与“继续旧会话”的关系
- subagent、skills、rules 对上下文负载的影响

## 你需要掌握
- 每个 Claude Code session 都是新的上下文窗口，跨 session 不会自动继承聊天历史
- 但 `CLAUDE.md` 和 auto memory 会在新 session 启动时重新注入
- Claude 读文件、跑命令、拿到输出，都会继续消耗上下文
- Claude Code 会自动 compact，但自动 compact 不是万能的
- `paths:` 规则、子目录里的 `CLAUDE.md`、一些按需加载的内容，在 compact 之后可能会暂时丢失，直到再次触发
- subagent 的价值不只是“并行”，更重要的是“隔离上下文”

## 一、先建立正确心智模型
把 Claude Code 的上下文想成“工作台面”。

- 台面上先放的是启动时就要看的东西：系统提示、`CLAUDE.md`、auto memory、skill 描述、MCP 名称等
- 你一旦让它读文件、看日志、跑测试，这些材料会继续堆上来
- 台面快放不下时，它会先清理一些旧输出，再尝试做摘要压缩
- 如果压缩之后立刻又被超大文件或超长输出塞满，就会进入“怎么 compact 都不够”的状态

所以，上下文管理不是“最后再救火”，而是从任务开始时就要设计。

## 二、哪些东西最容易把上下文撑爆
最常见的不是聊天，而是下面几类东西：

### 1. 大文件
- 一次读很长的源码文件
- 直接把整份文档、整份日志、整份报错贴进来
- 在 monorepo 里反复读大量无关模块

### 2. 大输出
- 测试输出过长
- 构建日志过长
- `git diff`、`git log`、`npm install` 之类命令输出太大

### 3. 错误的“长期携带”
- 把只在本任务里有用的资料长期留在主会话
- 明明是一次性检索任务，却一直在主线程里做
- 把流程说明写在聊天里，而不是沉淀到 `CLAUDE.md` / skill / rules

## 三、`/compact` 解决的是什么问题
`/compact` 的作用不是“清空聊天”，而是“把当前会话压缩成更短的结构化摘要”，让 Claude 还能继续干活。

你可以把它理解成：
- 保留任务主线
- 丢掉一部分细枝末节
- 重新给后续回合腾空间

它适合：
- 会话已经很长，但任务还没做完
- 你希望 Claude 记住“我们做到哪了”，但不需要保留所有中间细节
- 你已经发现响应开始变慢、变散、开始忘记局部背景

它不适合：
- 你还没把长期规则沉淀到 `CLAUDE.md`
- 你依赖某些路径触发的 rules 或子目录 `CLAUDE.md` 持续生效，却没有重新触发它们
- 当前上下文已经严重超限，compact 自己都塞不下摘要

## 四、compact 之后哪些东西会回来，哪些不一定
截至 **2026-04-25**，官方文档强调：

- 项目根级 `CLAUDE.md` 与不带 `paths:` 的 rules 会从磁盘重新注入
- auto memory 会重新注入
- 已调用的 skill 会在预算内重新挂回去，但可能截断，也可能因为太旧被挤掉
- 子目录里的 `CLAUDE.md`、带 `paths:` 的 rules，不会无条件自动常驻；它们通常要等你再次读到对应文件才会重新生效

这意味着一个很实用的原则：

**凡是“整个任务都必须一直记住”的规则，尽量放在项目根级 `CLAUDE.md` 或无路径限制的 rules。**

## 五、`/clear`、`/compact`、新开 session 的区别
这三个动作很多人会混。

### `/compact`
适合“任务没结束，但上下文太重了”。

### `/clear`
适合“我要立刻开始一个新的干净对话”。  
旧会话并没有消失，只是不再挂在当前交互里。之后仍可通过恢复会话的方式回到之前的内容。

### 新开 session
适合：
- 任务本身已经切换
- 当前任务的历史包袱太重
- 你不想让旧任务噪音带到新问题里

经验上：
- 同一目标、同一主线：优先继续当前会话或 compact
- 新目标、新问题域：直接开新会话

## 六、`--continue`、`--resume` 解决的是“恢复旧会话”，不是 slash command
这也是常见混淆点。

- `claude --continue`：继续最近一次会话
- `claude --resume`：打开会话选择器，恢复某个旧会话

它们是 **CLI 启动参数**，不是 slash command。

因此不要把它和 `/compact`、`/clear` 混在一起理解：
- `/compact` 是在当前会话内压缩上下文
- `--continue` / `--resume` 是重新进入一个历史会话

## 七、如何主动设计“轻上下文”工作流
真正高效的用法不是等爆了再 compact，而是一开始就减载。

### 1. 把长期规则前置
把这些放进 `CLAUDE.md`：
- 构建与测试命令
- 项目结构
- 编码规范
- 常见坑
- “永远不要做什么”

不要每次都在聊天里重复。

### 2. 把流程型知识放进 skill
如果某套步骤不是每个任务都要用，就不要塞进 `CLAUDE.md`。

例如：
- 发布流程
- PR review 流程
- 故障排查 checklist
- 某个内部系统的操作 SOP

这类内容更适合 skill，因为它是按需加载。

### 3. 把高噪音检索交给 subagent
适合交给 subagent 的任务：
- 大范围搜索
- 长日志排查
- 阅读大量文件后做总结
- 技术选型资料归纳

因为 subagent 在独立上下文里工作，主会话只拿结果摘要，不把大量原始材料都背在身上。

### 4. 少贴大段原始输出，多先裁剪
不要默认把 1000 行日志直接扔给 Claude。

更好的做法是先缩小范围：
- 只给失败测试
- 只给相关 stack trace
- 只给 diff 的关键片段
- 只读相关目录而不是整个仓库

## 八、你最该学会的几个判断题
### 什么时候该 `/compact`
- 当前任务还没做完
- 主线需要保留
- 但上下文明显变重了

### 什么时候该 `/clear` 或直接新开 session
- 任务已经切换
- 当前对话里有大量旧噪音
- 你希望彻底隔离上下文

### 什么时候该写进 `CLAUDE.md`
- 下次还会重复说
- 应该贯穿整个任务
- 属于事实、规范、长期约束

### 什么时候该改成 skill / subagent
- 只在部分任务里需要
- 内容较长
- 过程噪音大
- 更适合按需调用

## 九、建议你在脑子里形成的“上下文控制链”
最推荐的顺序是：

1. 先把长期规则放到 `CLAUDE.md`
2. 再把高频流程拆成 skills
3. 再把高噪音任务交给 subagents
4. 会话变重时用 `/compact`
5. 任务切换时直接新开 session

这比“所有东西都堆在一个超长聊天里”稳定得多。

## 十、和后续章节的关系
- 第 6 章会讲：什么内容该写进 `CLAUDE.md`，什么该交给 auto memory、rules、`.claude/`
- 第 7 章会讲：settings 是硬配置，决定模型、权限、hooks、环境变量等
- 第 8 章会讲：权限模式与 sandbox 如何影响 Claude 能做什么
- 第 9 章会讲：如何把这些能力变成稳定的日常工作习惯

## 学完标准
- 你不再把“上下文窗口”简单理解成聊天记录
- 你知道什么内容会在启动时自动进入上下文
- 你能区分 `/compact`、`/clear`、新开 session、`--resume`
- 你能主动把长期知识、临时流程、高噪音任务分流到正确机制里
