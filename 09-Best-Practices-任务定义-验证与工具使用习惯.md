# 9. Best Practices：任务定义、验证与工具使用习惯

## 重要程度
**S 级** - 必须掌握。同样的 Claude Code，有的人越用越稳，有的人越用越乱，差别通常不在模型，而在任务定义、验证闭环和上下文管理。

## 学习目标
- 学会把“帮我做一下”改成可执行任务
- 知道什么时候该先探索、什么时候该直接改
- 建立目标、范围、约束、验证四件套
- 把 memory、permissions、subagents、skills、hooks 用在正确场景

## 先建立一个场景
你让 Claude Code “修一下登录问题”。它可能会：
- 搜错文件
- 改错层级
- 绕过真实报错
- 只修表象
- 最后给你一个看似合理但没验证的结果

更好的任务不是更长，而是更具体：

```text
登录过期后刷新页面会跳回登录页，但重新登录后没有回到原页面。
请先定位 session restore 流程，不要直接重写 auth。
修复后补一个相关测试，并跑 auth 相关测试确认通过。
```

这就是 best practices 的核心：给 Claude 一个清楚的目标、边界和自证方式。

## 一、最高杠杆习惯：给验证方式
Claude Code 最怕的不是任务难，而是没有反馈闭环。

没有验证时，它只能给出“看起来对”的结果。
有验证时，它可以自己迭代，直到测试、构建、截图或输出符合预期。

常见验证方式：
- 单元测试、集成测试、e2e
- `lint`、`typecheck`、`build`
- 指定命令输出
- 页面截图或设计稿对比
- 明确的复现步骤

坏写法：

```text
帮我优化这个函数。
```

好写法：

```text
优化这个函数的重复分支，保持行为不变。
改完跑现有单测；如果没有覆盖，补充边界用例。
```

## 二、任务定义四件套
每次给 Claude Code 任务，优先补齐四件事：

- 目标：最终要实现什么
- 范围：重点看哪些文件或目录
- 约束：不能怎么做，必须沿用什么
- 验证：怎么证明完成了

模板：

```text
目标：修复用户退出后仍能看到缓存页面的问题。
范围：重点检查 `src/auth` 和路由守卫。
约束：不要引入新状态库，沿用现有 session 机制。
验证：补一个失败用例，修复后跑 auth 相关测试。
```

这比堆很多形容词更有效。

## 三、真实场景：修 bug 怎么说
不要只说“报错了”。要给症状、复现和验证。

```text
运行 `pnpm test auth` 时，`refresh token expired` 用例失败。
请先复现并定位根因，不要先改实现。
如果是测试过期，说明原因；如果是逻辑问题，补充失败用例后修复。
最后重新跑 auth 测试。
```

关键点：
- 先复现
- 再定位
- 再修
- 最后验证

如果你跳过复现，Claude 很容易直接猜。

## 四、真实场景：重构怎么说
重构最重要的是行为不变。

```text
把 `src/api/client.ts` 里的重复错误处理抽出来。
保持外部 API 行为不变，不要顺手改返回结构。
每次小步修改，最后跑 api client 相关测试和 typecheck。
```

要明确告诉 Claude：
- 不要重写
- 不要引入新库
- 不要顺手改业务行为
- 先找现有模式
- 改完必须验证

否则它可能把“重构”理解成“重新设计”。

## 五、真实场景：UI 修改怎么说
UI 任务不要只说“好看一点”。

更好的说法是：

```text
把设置页改得更适合桌面端管理后台。
范围是 `src/pages/settings` 和已有组件，不引入新 UI 库。
视觉方向：信息分组更清楚，主要操作更突出，减少表单拥挤。
完成后说明改了哪些交互层级，并跑 typecheck。
```

如果有截图或设计稿，直接给图。视觉任务靠文字猜，很容易偏。

## 六、什么时候先探索，什么时候直接做
先探索适合：
- 你不熟悉代码库
- 涉及多个模块
- 需要理解业务链路
- 可能有多种方案
- 失败成本高

直接做适合：
- typo
- 文案
- 单文件小改
- 明确的局部 bug
- 已经给出具体文件和改法

判断标准：

**如果你能一句话描述最终 diff，可以直接做；如果不能，先让 Claude 探索和计划。**

## 七、先探索的好提示
探索不是让 Claude 无限看代码，而是带边界地调查。

```text
先不要改代码。
请调查登录态恢复流程，重点看路由守卫、session storage、auth API。
输出：相关文件、调用链、可能问题点、建议修改方案。
```

这个提示有三个好处：
- 不会过早改文件
- 调查范围清楚
- 输出格式能直接进入下一步实现

## 八、上下文管理是日常稳定性的底层能力
很多“Claude 变笨了”的情况，本质是上下文脏了。

常见问题：
- 一个 session 混了太多无关任务
- 大日志、大文件、大 diff 塞进主上下文
- 失败尝试太多还继续纠偏
- `CLAUDE.md` 太长，重要规则被稀释

对应习惯：
- 新任务及时 `/clear` 或新开 session
- 长会话但同一主线，用 `/compact`
- 高噪音调查交给 subagent
- 大段日志先裁剪
- 长期规则沉淀到 memory，不要反复聊天解释

## 九、把能力放到正确位置
Claude Code 的稳定使用，不是靠一个超长提示词，而是靠分工。

- `CLAUDE.md`：长期项目事实和规则
- settings：模型、权限、环境变量、hooks 等硬配置
- permissions：低风险放行，高风险拦住
- hooks：每次都必须发生的自动动作
- skills：可复用流程
- subagents：高噪音调查或并行任务
- MCP / CLI：接入外部系统和真实工程工具

场景判断：
- 每次都要知道：写 memory
- 每次都要执行：写 hook
- 经常重复但不是每次都用：做 skill
- 会读很多文件：交给 subagent
- 需要真实系统数据：用 CLI 或 MCP

## 十、真实场景：让 Claude 反过来采访你
当需求很大时，不要直接让它开写。

比如：

```text
我想做一个团队权限管理模块。
先不要写代码。
请先采访我，问清楚角色、权限粒度、UI 入口、数据模型、边界条件和迁移风险。
问完后整理成 spec，再给实现计划。
```

这适合：
- 新功能
- 架构调整
- UI/UX 不确定
- 业务规则复杂

先把问题问清楚，比边做边改方向省很多上下文。

## 十一、常见失败模式
### 任务太模糊
表现：Claude 做了很多，但不是你要的。

修法：补目标、范围、约束、验证。

### 无验证
表现：结果看起来对，但你不敢合。

修法：给测试、构建、截图、复现步骤。

### 上下文太脏
表现：反复忘记重点，越改越偏。

修法：compact、clear、拆 session，或把调查交给 subagent。

### 权限太烦
表现：每条安全命令都要确认。

修法：把低风险高频命令加入 allow。

### 权限太松
表现：Claude 能碰到不该碰的文件或命令。

修法：补 deny、ask、sandbox，不要靠口头提醒。

## 十二、你可以直接复用的提示模板
### 修 bug
```text
这里有一个可复现问题：...
请先复现并定位根因，不要直接猜。
修复后补测试，并跑相关测试验证。
```

### 重构
```text
请重构 ...，目标是 ...
保持外部行为不变，不引入新依赖。
按现有代码风格小步修改，最后跑相关测试。
```

### 新功能
```text
目标：...
范围：...
约束：...
验证：...
如果信息不足，先问我，不要直接实现。
```

### 调查代码
```text
先不要改代码。
请调查 ... 的实现链路。
输出相关文件、调用关系、风险点和建议方案。
```

## 十三、这一章真正要形成的习惯
把 Claude Code 当成工程协作者，而不是聊天窗口。

你的工作不是写很长的提示词，而是把任务定义清楚：
- 它要达成什么
- 它能动哪里
- 它不能怎么做
- 它如何证明自己做对了

这四件事清楚，Claude Code 的表现会稳定很多。

## 和前后章节的关系
- 第 6 章解决 Claude 应该长期知道什么
- 第 7 章解决 Claude Code 应该如何运行
- 第 8 章解决 Claude 能不能做、做之前会不会问
- 第 10 章会把这些原则落成日常开发场景模板

## 学完标准
- 你会默认给 Claude 目标、范围、约束、验证
- 你知道先探索、先计划、直接做分别适合什么任务
- 你知道把 memory、permissions、hooks、skills、subagents 放到正确位置
- 你能避免模糊任务、无验证、脏上下文、权限过松或过烦这些常见问题
