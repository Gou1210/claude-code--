# 9. Best Practices：任务定义、验证与工具使用习惯

## 重要程度
**S 级** - 必须掌握。Claude Code 的上限，不主要取决于“模型聪不聪明”，而主要取决于你会不会给它正确的任务边界、验证闭环和上下文环境。

## 学习目标
- 理解官方 best practices 背后的核心约束其实是上下文管理
- 学会把“提需求”升级成“给目标 + 给约束 + 给验证 + 给环境”
- 建立一套低返工、低打断、可复用的 Claude Code 使用习惯

## 学什么
- Best Practices 官方页面
- 如何给 Claude 一个可自证正确的验证方式
- 如何先探索、再规划、再执行
- CLAUDE.md、permissions、CLI、MCP、hooks、skills、subagents 这些能力怎么组合
- session、context、checkpoints、resume、auto mode 的正确使用方式

## 你需要掌握
- 官方最强调的一点不是“提示词技巧”，而是 **给 Claude 一个可验证结果的方法**
- 最重要的资源不是时间，而是 **context window**
- Claude Code 最适合的协作方式不是“你写它看”，而是“你定义任务，它去探索、实施、验证”
- 很多效率问题，本质上不是模型不行，而是任务定义太模糊、上下文太脏、验证太弱

## 一、先抓住这章真正的总纲：上下文是第一资源
截至 **2026-04-25** 的官方 Best Practices 文档，几乎所有建议都围绕一个事实展开：

**Claude 的 context window 很快就会被对话、读过的文件、命令输出、失败尝试塞满，而性能会随之下降。**

所以你不应该把 best practices 理解成一堆零散技巧，而应该理解成三件事：

1. 少让无关内容进入上下文
2. 让进入上下文的内容尽量高质量
3. 给 Claude 可自检的反馈闭环，减少你人工兜底

## 二、最高杠杆习惯：给 Claude 验证方式
官方明确把这件事称为 **single highest-leverage thing**。

原因很简单：
- 没有验证时，Claude 只能生成一个“看起来像对的答案”
- 有验证时，Claude 可以自己迭代、自己修正、自己收敛

### 最好的验证方式有哪些
- 测试：单测、集成测试、e2e
- 明确输出：命令应输出什么、接口应返回什么
- UI 对比：截图、设计稿、视觉差异
- 构建与静态检查：lint、typecheck、build

### 坏任务写法
- 帮我实现一个邮箱校验函数
- 帮我把页面改好看一点
- 构建报错了，你修一下

### 更好的写法
- 写一个 `validateEmail`，并验证 `user@example.com=true`、`invalid=false`、`user@.com=false`，改完跑测试
- 根据这张截图实现页面，并在完成后截图对比差异，再修到一致
- 构建在这个错误处失败，修复根因，不要绕过报错，最后确认 build 成功

## 三、推荐工作节奏：先探索，再规划，再编码
官方推荐的典型节奏是四段：

1. Explore：先读代码、理解现状
2. Plan：先产出实施计划
3. Implement：按计划修改并验证
4. Commit：整理改动并产出提交 / PR

这背后的价值是：
- 避免一上来就改，结果改错方向
- 避免边想边写，导致任务定义漂移
- 避免你脑子里想的是 A，Claude 实际实现成了 B

### 什么时候值得先 plan
- 涉及多文件联动
- 你自己对代码库不熟
- 需要做架构或流程层面的改动
- 你还不确定方案

### 什么时候可以直接做
- 修 typo
- 改文案
- 加一个日志
- 明确且局部的小改动

**判断标准很实用：如果你能用一句话描述 diff，通常可以直接做；如果不能，先 plan。**

## 四、好提示词不是“华丽”，而是“具体”
Claude 能推断意图，但不会读心。

所以高质量任务一般至少包含四个部分：
- 目标：要做成什么
- 范围：改哪些文件、哪些模块
- 约束：不要怎么做，要遵循什么模式
- 验证：改完如何确认是对的

### 官方建议你提供的具体信息
- 指定文件或目录
- 指定症状而不是只说“有 bug”
- 指向已有模式或参考实现
- 指定测试偏好，例如避免 mock、只跑某类测试
- 给出报错、日志、截图、链接、文档

### 一个高质量任务模板
```text
目标：在 auth 模块里加入 xxx 能力
范围：重点看 src/auth/ 和 callback handler
约束：沿用现有 session flow，不引入新库
验证：写失败用例复现问题，修复后跑相关测试并通过
```

## 五、Rich Context 比长篇解释更重要
官方建议尽量给 Claude 可直接消费的材料，而不是让它猜你在说什么。

高价值输入包括：
- 用 `@文件` 或 `@目录` 直接引用代码
- 直接贴截图、设计稿、错误界面
- 贴 URL 让它读取文档
- 用管道把日志、报错、数据喂给 Claude
- 让 Claude 自己去读文件、跑命令、调用 MCP

这意味着：

**你要少写“这个模块大概在某个地方”，多写“看 `@src/auth`、`@src/session.ts`，按现有模式实现”。**

## 六、环境配置不是附属品，而是效率基建
官方 best practices 在“配置环境”这一段，实际上是在说：

**别把 Claude Code 当纯聊天工具，而要把它变成一个真正能稳定工作的工程环境。**

### 1. 写好 `CLAUDE.md`
`CLAUDE.md` 适合放：
- Claude 读代码看不出来的信息
- 项目特有规范
- 常用命令
- 测试方式
- 分支、PR、仓库礼仪
- 易踩坑说明

不适合放：
- 长教程
- 会频繁变化的细节
- Claude 读代码就能知道的事实
- 大段 API 文档

一句话记忆：

**`CLAUDE.md` 放长期稳定规则，别放偶发流程。**

### 2. 配好 permissions
官方建议你用三种方式降低审批疲劳：
- `auto mode`
- allowlists
- sandboxing

重点不是“越严格越好”，而是：

**让低风险常用动作少打断，让高风险动作仍然受控。**

### 3. 让 Claude 用 CLI，而不是总走网页接口
官方明确建议配合这些 CLI：
- `gh`
- `aws`
- `gcloud`
- `sentry-cli`

原因是：
- CLI 通常更省上下文
- 更接近真实开发环境
- 权限和认证流程也更清晰

### 4. 需要外部系统时用 MCP
如果任务需要：
- 读设计稿
- 查 issue
- 访问数据库
- 看监控
- 连 Notion / Figma / Slack

就别强行靠聊天描述，应该接 MCP。

### 5. 需要“每次都必须发生”的动作时用 hooks
官方特别强调：
- `CLAUDE.md` 是建议
- hooks 是强制执行

所以像这些需求更适合 hooks：
- 每次编辑后自动格式化
- 阻止写某些敏感目录
- 记录工具调用
- 自动提醒 / 通知

### 6. 可复用流程用 skills
适合 skill 化的，是：
- 反复出现的流程
- 项目知识
- 容易写成长提示词的套路

### 7. 高噪音调查用 subagents
适合 subagent 的，是：
- 会读很多文件的调查
- 会产生很多输出的验证
- 需要专门角色的任务

## 七、沟通方式要像和高级工程师协作
官方有两个很重要的建议。

### 1. 直接问代码库问题
你可以像问同事一样问：
- 日志怎么走的
- 新接口通常怎么建
- 为什么这里用 `foo()` 不用 `bar()`
- 某个类处理了哪些边界情况

这类问题不需要复杂提示词，关键是问得具体。

### 2. 大功能先让 Claude 反过来采访你
官方建议对大需求可以先让 Claude interview 你，把：
- 技术实现
- UI/UX
- 边界条件
- tradeoff
- 风险点

都问清楚，再把 spec 写出来。

这比“边聊边做边改方向”更稳。

## 八、session 管理能力，决定你会不会越聊越废
很多人用久了觉得 Claude 变笨，常见原因不是模型降级，而是 session 被污染了。

### 官方建议你这样做
- 一发现走偏就尽早纠正
- 同一问题纠偏两次还不对，就 `/clear` 重开
- 不相关任务之间及时 `/clear`
- 需要保留重点时用 `/compact`
- 让 subagents 去做调查，避免把主上下文塞满
- 善用 checkpoints 和 `/rewind`
- 用 `claude --continue`、`--resume`、`/resume` 续接上下文
- 及时给 session 命名，避免以后找不到

### 你应该形成的判断
- 同一主题、同一问题链路：可以保留上下文
- 完全无关的新任务：应该新开或 clear
- 输出特别多的工作：应该拆给 subagent

## 九、自动化和并行，是进阶使用分水岭
官方 best practices 后半段明显不再只是“如何聊天”，而是在讲如何把 Claude Code 变成工程流水线的一部分。

### 1. 用非交互模式
`claude -p "prompt"` 适合：
- CI
- pre-commit
- 脚本
- 一次性检查

### 2. 跑多个 Claude session
适合：
- Writer / Reviewer 分工
- 一边实现，一边独立 review
- 一边写代码，一边写测试

### 3. 大规模任务用 fan-out
比如：
- 批量迁移文件
- 批量分析目录
- 批量生成修复结果

### 4. 需要少打断时用 auto mode
官方现在把 auto mode 放在较核心的位置，它不是放飞，而是：

**让后台分类器拦危险动作，放常规动作。**

## 十、官方点名的几种常见失败模式
### 1. Kitchen sink session
一个会话里混进太多无关任务。

修法：
- `/clear`
- 拆 session

### 2. 一错再错还在原地纠偏
上下文里积累了太多失败尝试。

修法：
- 两次纠偏还不对就重开

### 3. `CLAUDE.md` 过长
规则太多，真正重要的被淹没。

修法：
- 删
- 压缩
- 把“必须执行”的规则改成 hook

### 4. 只相信表面结果，不验证
实现看起来合理，实际上边界全错。

修法：
- 永远给验证

### 5. 无限探索
让 Claude 去“研究一下”，但没给边界。

修法：
- 缩范围
- 或交给 subagent

## 十一、这一章真正要建立的使用观
最推荐的心智模型是：

1. 我不是在“向 AI 提问”，而是在“给一个代理定义任务”
2. 任务定义里最重要的是验证，而不是形容词
3. 任务跑偏时，先怀疑上下文和任务定义，再怀疑模型
4. 能自动化的尽量自动化，能隔离的尽量隔离

## 十二、和前后章节的关系
- 第 8 章解决的是“Claude 能不能做、做之前会不会问”
- 第 9 章解决的是“同样能做，为什么有的人越用越稳，有的人越用越乱”
- 第 10 章会把这些原则落成常见工作流模板

## 学完标准
- 你会默认给 Claude 目标、范围、约束、验证方式
- 你知道 context window 才是日常使用中的第一约束
- 你知道什么时候该 clear、compact、resume、rewind
- 你知道 `CLAUDE.md`、permissions、CLI、MCP、hooks、skills、subagents 各自该解决什么问题
- 你已经开始把 Claude Code 当成工程协作环境，而不是聊天窗口
