# 10. Common Workflows：日常开发场景模板

## 重要程度
**A 级** - 强烈建议掌握。第 9 章讲原则，这一章讲怎么把原则变成你每天真的会用的工作流。

## 学习目标
- 把常见开发任务映射成稳定的 Claude Code 操作模板
- 知道每类任务应该怎么起手、怎么验证、怎么收尾
- 学会在探索、实现、验证、并行、恢复之间切换

## 学什么
- 理解新代码库
- 找代码、追流程、定位问题
- 修 bug、重构、补测试、写文档、提 PR
- plan mode、subagents、thinking、resume、worktree
- 非交互执行、管道、输出格式、定时任务

## 你需要掌握
- 官方 `Common workflows` 页面不是“功能清单”，而是“场景模板库”
- Claude Code 最强的不是回答，而是能把探索、修改、验证串成一个连续流程
- 真正高效的人，不是每次重新想提示词，而是把高频任务固化成套路

## 一、理解新代码库：先广后深
适用场景：
- 刚接手项目
- 刚切到一个陌生模块
- 要评估改动成本

### 推荐起手提示
```text
先给我一个这个代码库的整体概览。
重点讲目录结构、主流程、核心模块、关键数据模型。
```

### 第二轮继续问
```text
解释这里主要用了哪些架构模式？
认证流程怎么走？
哪些目录是高风险改动区？
```

### 这类任务的关键
- 先要 overview，不要一上来就问单点实现
- 先建立词汇表和模块地图
- 再逐步深入具体组件

## 二、找相关代码：从“在哪”到“怎么串起来”
适用场景：
- 你知道要改什么功能，但不知道代码在哪
- 你知道文件名，但不知道执行链路

### 推荐模板
```text
找出处理用户认证的文件。
然后说明这些文件如何协作。
最后从前端入口一路追到后端 / 数据库。
```

### 要点
- 第一步找文件
- 第二步讲关系
- 第三步讲执行流

如果你直接问“登录怎么修”，Claude 可能会过早进入解决模式；先让它把链路讲清楚，成功率更高。

## 三、修 bug：先复现，再定位，再修，再验证
这是最常见、也最容易浪费时间的场景。

### 官方推荐节奏
1. 提供错误或症状
2. 给出复现方式
3. 让 Claude 分析可能原因
4. 再让它实施修复
5. 最后验证

### 推荐模板
```text
`npm test` 时出现这个错误：...
请先定位根因，不要先拍脑袋改。
写一个失败用例复现问题，再修复，并重新跑相关测试。
```

### 如果你已经有怀疑范围
```text
用户在 session 超时后登录失败。
重点检查 `src/auth/` 下 token refresh 相关逻辑。
先复现，再修，不要通过吞错掩盖问题。
```

## 四、重构：保持行为不变比“写新代码”更重要
适用场景：
- 旧 API 替换
- 老代码现代化
- 模块拆分
- 减少重复

### 推荐模板
```text
找出这个模块里过时的 API 用法。
给出重构建议。
然后按“行为不变”的前提逐步重构，并跑相关测试验证。
```

### 关键要求要明确写出来
- 保持兼容
- 小步改
- 每一步可验证
- 沿用现有模式，不乱引库

如果你不写“保持行为不变”，Claude 有时会把重构做成重写。

## 五、用 Plan Mode 做安全分析
官方现在把 Plan Mode 放在常见工作流里，不再只是“只读模式”，而是：

**用于探索复杂问题、先出计划、降低误改风险的工作模式。**

### 适合用 Plan Mode 的情况
- 多文件改动
- 架构调整
- 你自己对方案还不确定
- 你想先讨论路线，不想先改文件

### 用法
- 会话里切换到 plan mode
- 或直接：

```bash
claude --permission-mode plan
```

### 非交互分析
```bash
claude --permission-mode plan -p "分析认证系统并给出改进建议"
```

### 典型流程
```text
我想把认证系统改成 OAuth2。
先不要改代码，先分析现状并给出详细迁移计划。
```

如果计划需要人工微调，可以再编辑计划后继续执行。

## 六、补测试：别只让它“加测试”，要让它“找风险点”
适用场景：
- 覆盖率不够
- 刚修完 bug
- 刚完成重构

### 推荐模板
```text
找出 `NotificationsService.swift` 里还没被覆盖的函数。
按项目现有测试风格补测试。
重点加边界条件和错误路径用例。
改完跑测试并修复失败项。
```

### 高收益补充要求
- 指定不要 mock，或尽量少 mock
- 指定测试层级：单测 / 集成 / e2e
- 让 Claude 说明测试策略

## 七、创建 PR：让 Claude 先总结，再生成，再补风险说明
适用场景：
- 改动已经完成
- 你需要整理 PR 描述
- 你不想手写 changelog

### 推荐流程
1. 先让 Claude 总结改动
2. 再让它创建 PR
3. 再补充测试与风险说明

### 推荐模板
```text
先总结我对 authentication module 的改动。
然后创建一个 PR。
最后补充安全改进背景、测试方式和潜在风险。
```

### 官方补充点
- 如果通过 `gh pr create` 创建 PR，会话会与 PR 关联
- 后续可以用 `claude --from-pr <number>` 恢复相关会话

## 八、文档工作流：Claude 不只适合写代码
适用场景：
- 补 JSDoc / docstring
- 补模块说明
- 更新 README / 使用文档

### 推荐模板
```text
找出 auth 模块里缺少文档的函数。
按项目现有文档风格补齐。
对复杂逻辑补充示例和上下文。
最后检查是否符合我们的文档规范。
```

## 九、非代码目录也能用 Claude Code
官方专门说明：

**Claude Code 不只适合源码仓库，也适合 markdown、笔记、文档目录。**

适用场景：
- 知识库整理
- 大量 markdown 重构
- 笔记归类
- 文档统一术语

这也是你当前这个仓库最直接的应用场景。

## 十、图片工作流：截图、设计稿、错误界面都可以直接喂
适用场景：
- UI 对齐
- 看报错截图
- 解读架构图
- 根据设计稿生成结构 / CSS

### 常见问法
```text
这张图里有什么 UI 元素？
这个错误截图暴露了什么问题？
根据这张设计稿生成对应的 HTML / CSS 结构。
```

### 什么时候值得上图
- 文字描述费劲
- 你怕 Claude 理解偏
- 你需要它做视觉对比

## 十一、`@文件` 和 `@目录` 是高频必用手法
官方把它放进 common workflows，是因为它非常高效。

### 典型用法
```text
解释 `@src/utils/auth.js` 的核心逻辑
```

```text
`@src/components` 的结构是什么？
```

### 你需要知道
- `@文件` 会把文件内容带进上下文
- `@目录` 主要提供目录结构，不直接带全文
- `@文件` 会顺带把相关目录层级的 `CLAUDE.md` 带入上下文

## 十二、Thinking / Effort：复杂任务要让模型真的“多想一点”
官方现在把 extended thinking 默认启用，并引入 effort / adaptive reasoning 相关配置。

### 你该怎么理解
- 简单任务不需要过度思考
- 复杂分析、架构比较、难 bug、长链路规划，值得更高 effort

### 实操重点
- 需要更深推理时，显式提高 effort
- 不要把“think harder”当成唯一手段
- 重点是给复杂任务匹配更高思考预算，而不是在所有任务上都拉满

## 十三、恢复会话：把 session 当分支管理
官方把 resume 体系写得很完整，说明这已经是核心工作流。

### 常用命令
- `claude --continue`：继续当前目录最近一次会话
- `claude --resume`：打开选择器
- `claude --resume <name>`：按名称恢复
- `claude --from-pr <number>`：按 PR 恢复

### 最佳实践
- 一开始就给 session 命名
- 一个任务一个名字
- 别把所有事情都挤进一个会话

### 推荐命名方式
- `auth-refactor`
- `debug-session-timeout`
- `docs-mcp-overview`

## 十四、并行工作流：用 worktree 隔离多个 Claude
官方现在把 Git worktree 作为并行工作的标准方案之一。

### 适用场景
- 多任务同时推进
- Writer / Reviewer 并行
- 两个大分支方向同时试

### 启动方式
```bash
claude --worktree feature-auth
```

这会在仓库下创建隔离工作树，并启动单独会话。

### 你需要知道
- worktree 默认从本地 `origin/HEAD` 指向的默认分支分叉
- 子任务、并行会话、某些 subagent 也可以使用 worktree 隔离
- `.env` 这类 gitignored 文件默认不会跟着过去，可以用 `.worktreeinclude` 控制复制

## 十五、长任务提醒：用 Notification hooks
适用场景：
- 你让 Claude 跑长任务后切到别的窗口
- 你不想一直盯着终端

官方建议通过 `Notification` hook 做：
- 请求权限时提醒
- 完成后提醒
- 认证完成时提醒
- Claude 需要你回答问题时提醒

这类能力在长流程里很值钱。

## 十六、把 Claude 当 Unix 工具，而不是聊天面板
这是官方很有代表性的一类 workflow。

### 1. 把 Claude 接到验证流程里
例如在 `package.json` 中加：
- 让 Claude 做 typo review
- 做简单静态检查
- 做变更总结

### 2. Pipe in / Pipe out
```bash
cat build-error.txt | claude -p "简洁解释这个构建错误的根因" > output.txt
```

### 3. 控制输出格式
- `text`
- `json`
- `stream-json`

这意味着 Claude Code 可以嵌进：
- shell 脚本
- CI/CD
- 自定义工具链

## 十七、定时任务：把周期性工作交给 Claude
官方列出的几个方向：
- 每天 review 开放中的 PR
- 每周检查依赖
- 夜间巡检 CI 失败

常见载体包括：
- Routines
- Desktop scheduled tasks
- GitHub Actions
- `/loop`

这里先知道用途即可，详细机制会在后续章节展开。

## 十八、这一章最值得你沉淀下来的模板
### 新项目上手模板
```text
先给我这个仓库的整体结构、关键模块、主流程和术语表。
然后重点展开 xxx 模块。
```

### 修 bug 模板
```text
这里有一个可复现错误：...
先复现并定位根因，再修复，最后跑相关测试验证。
```

### 重构模板
```text
找出这个模块里过时实现。
按“行为不变”的前提小步重构，并在每一步验证。
```

### 补测试模板
```text
找出未覆盖逻辑，按现有测试风格补用例，重点覆盖边界与异常路径。
```

### PR 模板
```text
先总结改动，再创建 PR，并补充测试方式、风险点和回滚考虑。
```

## 十九、和前后章节的关系
- 第 9 章讲的是使用原则
- 第 10 章讲的是把原则落成场景模板
- 第 11 章开始进入“如何把这些模板沉淀成可复用能力”

## 学完标准
- 你能为新项目上手、修 bug、重构、补测试、提 PR 各说出一套固定起手模板
- 你知道什么时候该用 plan mode、resume、worktree、notifications、pipe、非交互模式
- 你已经不再把 Claude Code 当成“问答工具”，而是当成“可编排的开发工作流引擎”
