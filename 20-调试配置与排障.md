# 20. 调试配置与排障

## 重要程度
**B 级** - 有场景时重点学。大多数“Claude 怎么没按我配置来”的问题，不是模型失灵，而是配置没加载、被覆盖、写错位置，或者你以为生效其实没生效。

## 学习目标
- 学会系统化排查 `CLAUDE.md`、settings、hooks、skills、MCP、subagents 为什么没生效
- 掌握 `/context`、`/doctor`、`/hooks`、`/mcp`、`/permissions` 等诊断入口
- 能区分“没加载”“被覆盖”“写错了”“描述不够好”这几类问题

## 学什么
- Debug your configuration
- `/context`
- `/memory`
- `/skills`
- `/agents`
- `/hooks`
- `/mcp`
- `/permissions`
- `/status`
- `/doctor`

## 你需要掌握
- 截至 **2026-04-25**，官方已经有单独的 `Debug your configuration` 文档
- 排障第一步不是猜，而是看 **Claude 实际加载了什么**
- 设置存在明确优先级：managed > local > project > user，再叠加 env / CLI 覆盖
- 很多问题和“规则写得差”无关，而是压根没被加载进来

## 一、这一章的总原则：先看实际加载，再谈提示词质量
当你遇到这些情况：
- `CLAUDE.md` 明明写了，Claude 没照做
- skill 放进去了，却不触发
- hook 配好了，却不执行
- MCP server 明明在配置里，却不可用

第一反应不应该是：
- 模型又忘了
- 提示词不稳

而应该是：

**先确认 Claude 到底加载了什么。**

这就是官方 `Debug your configuration` 文档的核心思路。

## 二、排障第一入口：`/context`
官方明确建议：

**先跑 `/context`。**

它会告诉你当前 context window 里到底有什么，按类别拆开。

典型类别包括：
- system prompt
- memory files
- skills
- MCP tools
- conversation messages

它的价值在于：
- 先确认东西有没有进来
- 再讨论为什么行为不符合预期

如果一个东西根本没出现在 `/context` 里，就别再争论“它为什么没听懂”了。

## 三、针对不同问题，去看不同面板
官方当前给出一整套分诊命令，这套图非常值得记住。

### `/memory`
看哪些 `CLAUDE.md`、rules、auto-memory 被加载了。

### `/skills`
看当前可用 skills 来自哪里。

### `/agents`
看 subagents 的配置和状态。

### `/hooks`
看当前 session 注册了哪些 hooks。

### `/mcp`
看连接了哪些 MCP servers，以及状态如何。

### `/permissions`
看当前生效的 allow / deny 规则。

### `/doctor`
看配置诊断、schema 错误、安装健康状况。

### `/status`
看当前 settings 来源，以及是否有 managed settings。

## 四、调试 `CLAUDE.md` / memory 的常见思路
如果你怀疑 `CLAUDE.md` 没生效，先做两件事：

1. 看 `/memory`
2. 看它所在路径是否真的会被当前会话加载

很多问题出在这里：
- 文件放错目录
- 你以为子目录规则会全局生效
- 你以为 `--add-dir` 目录里的 `CLAUDE.md` 会自动加载

实际上官方当前明确说明：
- `--add-dir` 默认不加载这些目录里的 `CLAUDE.md`
- 需要特定配置才会带入

所以路径问题永远是第一嫌疑。

## 五、settings 优先级不清，是最常见配置错觉
官方当前在 debug 文档里明确强调：
- settings 会 merge
- 但更近的 scope 会覆盖更远的 scope
- managed settings 优先级最高

可以把它记成：

`managed > local > project > user`

另外：
- 环境变量
- CLI flags

也可能作为覆盖层存在。

所以当你说“我明明在 `settings.json` 写了”时，真正的问题常常是：

**你写了，但被别的地方盖掉了。**

## 六、一个高频坑：`~/.claude.json` 不是 `~/.claude/settings.json`
官方 debug 文档专门点了这个坑。

很多人会把：
- permissions
- hooks
- env

写进 `~/.claude.json`

但这些应该写在：
- `~/.claude/settings.json`

这是两个完全不同的文件。

所以如果全局配置怎么都不生效，先检查是不是写错地方。

## 七、skill 为什么“不出现”与“出现但不触发”，是两类问题
这点要分开查。

### 情况 1：skill 根本不在 `/skills`
优先排查：
- 目录结构是否正确
- 是否应该是 `.claude/skills/name/SKILL.md`
- 而不是 `.claude/skills/name.md`

### 情况 2：skill 在 `/skills`，但 Claude 不会自动调用
优先排查：
- `disable-model-invocation: true` 是否开了
- `description` 是否写得过于抽象
- 你的请求措辞是否和 `description` 不匹配

官方当前还提醒：
- 如果 skill 很多，description 会被裁短
- 关键词位置太靠后，可能导致触发变差

这不是加载问题，而是匹配问题。

## 八、hook 排障：先看有没有注册，再看 matcher
这是官方给出的很实用的路径。

### 第一步
跑 `/hooks`

如果 hook 根本没出现在列表里，通常说明：
- 配置没被读到
- JSON schema 错了
- 没放在 `"hooks"` key 下

### 第二步
如果出现在 `/hooks` 里，但没触发，优先怀疑 `matcher`

官方明确提示：
- `matcher` 是单个字符串
- 多工具用 `|`
- 拼错工具名会静默失效
- 数组值会触发 schema error

例如：
- `"Edit|Write"` 是对的
- `["Edit", "Write"]` 是错的

## 九、hook 调试日志也很关键
官方 hooks reference 当前说明：
- 可以用 `claude --debug`
- 或 `claude --debug-file <path>`

去看 hook 详细执行日志。

其中会包含：
- 哪些 hooks 命中了
- exit code
- stdout / stderr

如果 `/hooks` 看起来正常，但行为不对，下一步就该看 debug log，而不是继续盲改配置。

## 十、MCP 排障：先看连接状态，不要先怀疑 prompt
MCP 常见问题大致有三类：
- server 没启动
- server 连上了但工具数为 0
- 配置被别的 scope 覆盖

官方当前建议的路径是：
1. 跑 `/mcp`
2. 看 server 状态
3. 需要时做 reconnect
4. 如果工具仍然是 0，再用 `claude --debug mcp` 看 stderr

这很重要，因为 MCP 问题大多是连接和配置问题，不是模型理解问题。

## 十一、subagent 为什么“不见了”
先看：
- `/agents`

如果 agent 不在里面，优先排查：
- 文件路径
- YAML frontmatter
- scope 来源
- 是否被更高优先级同名 agent 覆盖

如果 agent 在，但没有自动委派，优先排查：
- `description`
- 任务描述是否匹配
- 工具权限是否过窄导致无法执行

这里也要区分：
- 发现问题
- 触发问题

## 十二、permissions 问题常见不是“没权限”，而是“规则解析与你想的不一样”
调试权限时，先跑：
- `/permissions`

看最终 resolved rules。

因为很多时候你以为自己写的是：
- allow 某命令

实际匹配却不是你想的。

尤其要警惕：
- Bash 规则写法
- 优先级
- 具体 specifier 语法

如果不看 resolved state，只看原始 JSON，很容易继续误判。

## 十三、`/doctor` 该在什么时候用
官方当前对 `/doctor` 的定位是：
- 看 schema error
- 看无效 key
- 看安装和配置健康状态

所以它最适合这些场景：
- 改完配置后不确定是否合法
- 某功能完全不出现
- Claude 提示 settings error
- 你怀疑安装本身有问题

`/doctor` 很像一个总检面板，但它不能替代 `/context`。

最稳的顺序通常是：
- `/context`
- 再看专项命令
- 再看 `/doctor`

## 十四、配置没生效，通常就四类根因
这章最推荐你记住这四分类。

### 1. 没加载
文件路径不对、scope 不对。

### 2. 被覆盖
更近 scope、managed settings、env 或 CLI flags 覆盖。

### 3. 写错了
schema 错误、字段位置不对、matcher 不对。

### 4. 加载了，但匹配不到
skill description 不匹配、agent description 太泛、hook matcher 拼错。

这四类分清楚之后，90% 的“为什么它没按规则做”都能更快定位。

## 十五、别把“加载问题”和“提示词质量问题”混在一起
一个很常见的误区是：
- 规则没触发
- 立刻开始改 wording

但如果东西根本没加载，改 wording 没意义。

排障顺序应该是：
1. 它加载了吗
2. 它被覆盖了吗
3. 它 schema 对吗
4. 它匹配逻辑对吗
5. 最后才是 wording 优化

## 十六、针对这个仓库类型，哪些排障最常见
你这个仓库是 Markdown 知识库，实际最常见的问题通常会是：
- `CLAUDE.md` 或 rules 没按目录生效
- skills/commands 路径放错
- hooks 想当然地以为会自动生效
- permissions 规则和预期不一致

所以对这种仓库，最常用的诊断命令往往就是：
- `/memory`
- `/skills`
- `/hooks`
- `/permissions`

## 十七、这一章真正要形成的习惯
以后只要出现“为什么没按我说的做”，默认先查：
- 它有没有加载
- 它从哪加载
- 它被谁覆盖
- 它最终解析成什么

而不是一上来就重新解释一遍规则。

## 十八、和前后章节的关系
- 第 19 章讲的是回滚与恢复
- 第 20 章讲的是配置诊断和行为排障
- 第 21 章会把视角扩大到 GitHub、CI/CD 和企业集成

## 学完标准
- 你知道 `/context` 是第一排障入口
- 你知道 `/memory`、`/skills`、`/agents`、`/hooks`、`/mcp`、`/permissions`、`/doctor` 各该在什么问题下使用
- 你能区分没加载、被覆盖、写错了、匹配不到这四类问题
- 你遇到“它为什么没按规则做”时不再凭感觉瞎改
