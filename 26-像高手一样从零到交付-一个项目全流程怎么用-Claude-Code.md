# 26. 像高手一样从零到交付：一个项目全流程怎么用 Claude Code

## 重要程度
**S 级** - 极其重要。如果你不是想再背一堆功能，而是想知道：

**一个高手程序员，真的会怎样把 Claude Code 带进一个项目，从创建到交付一路用下去？**

这一章就是给你的。

## 学习目标
- 建立一个“项目全生命周期”的 Claude Code 使用模板
- 知道在不同开发场景下，Claude Code 应该扮演什么角色，而不是只会随手问几句
- 学会把 memory、skills、hooks、permissions、review、resume、worktree 这些能力放进正确阶段

## 学什么
- 项目初始化
- 新仓库上手
- 功能开发
- 修 bug
- 重构
- 补测试
- review
- commit / PR
- 长任务续跑
- 交付后沉淀 memory / skill / hook

## 你需要掌握
- 高手不是“想到什么就问什么”，而是每个阶段都有固定工作姿势
- Claude Code 最强的地方，不是回答问题，而是把探索、实现、验证、回顾串成一条流水线
- 真正高效的使用方式，是让 Claude Code 在不同阶段扮演不同角色：
  - 初始化时像 setup assistant
  - 开发时像实现工程师
  - 调试时像调查员
  - 评审时像 reviewer
  - 长任务里像可交接的执行代理

## 一、先说总原则：高手不会一直“聊天”，而是在切换工作模式
如果把一个真实项目压缩成一句话，高手通常这样用 Claude Code：

1. 项目开始前，先把环境和规则搭好
2. 接需求时，先让 Claude 理解代码和边界
3. 开发时，始终给验证闭环
4. 长任务里，主动 compact / clear / resume / handoff
5. 收尾时，单独做 review
6. 任务结束后，把重复纠偏沉淀成 memory、skill、hook

这六步，你看起来像只是“多做了点整理”，但它就是高手和普通用户的差距。

## 二、阶段 0：项目刚开始时，高手先做什么
这是最容易被忽略、但最能拉开差距的一步。

很多普通用户一进项目就直接问：
- 这个代码库是干嘛的？
- 帮我加个功能

高手通常不会这样开局。

### 高手开局第一件事：初始化项目记忆
如果这是一个新项目，最稳的起手是：

```text
/init
```

如果你开启了新版初始化流：

```bash
CLAUDE_CODE_NEW_INIT=1 claude
```

或者先进入 session，再跑 `/init`。

官方现在的新版 `/init` 已经不只是生成 `CLAUDE.md`，还会把：
- skills
- hooks
- personal memory

一起纳入初始化讨论。  
这一步非常值钱，因为它会把“以后每次都要重新解释”的信息，提前沉淀下来。

### 高手开局第二件事：先定三层，而不是写一大坨规则
高手不会上来就写一本超级长的 `CLAUDE.md`。

更典型的做法是：

#### 1. 全局 `~/.claude/CLAUDE.md`
只写你在所有项目都通用的偏好。

例如：
- 先探索再修改
- 默认给验证方式
- 优先用现有模式
- 输出简洁

#### 2. 项目 `CLAUDE.md`
写这个项目每次都该知道的事实。

例如：
- build / test / lint 命令
- 目录结构
- 约束与禁区
- 提交前最低验证要求

#### 3. `.claude/rules/`
把只对某部分代码生效的规则拆出去。

例如：
- `frontend/` 的 UI 规范
- `tests/` 的测试风格
- `docs/` 的文风

### 高手开局第三件事：检查 auto memory 是开着的
auto memory 默认开启，但高手通常会确认一次：

```text
/memory
```

看三件事：
- 当前加载了哪些 `CLAUDE.md`
- auto memory 是否启用
- auto memory 目录里已经记住了什么

这一步的意义是：

**很多人以为 Claude “应该记住”，但其实它根本没加载到那个记忆。**

## 三、阶段 1：刚进入一个仓库时，高手怎么快速上手
高手不会一上来就让 Claude 改代码。

他们更常见的第一轮，是让 Claude 做 **代码库地图构建**。

### 第一轮问法
```text
先给我这个仓库的整体概览。
重点讲目录结构、主流程、关键模块、构建方式、测试方式。
```

### 第二轮问法
```text
这个仓库里最重要的几个入口文件是什么？
认证、数据流、错误处理分别在哪些文件里？
哪些目录是高风险改动区？
```

### 第三轮问法
```text
如果我要改 xxx 功能，应该先看哪些文件？
请按“入口 -> 中间层 -> 持久化/外部依赖”给我一条追踪路径。
```

这三轮做完，Claude 就不再只是聊天对象，而是在帮你搭项目地图。

### 高手这时会顺手做的一件事
如果 Claude 反复提到某个重要事实，而项目 `CLAUDE.md` 里没有，高手会补进去。

比如：
- 某个服务必须先启动 Redis
- 某个测试要先做 seed
- 某个目录不能直接改

这类信息，不要等以后忘了再说。  
它已经证明是“每次都值得知道”的事实了。

## 四、阶段 2：真正接到任务时，高手不是直接做，而是先定任务壳
这是最重要的一步。

普通用户很容易这样说：
- 帮我把登录问题修一下
- 帮我改这个页面
- 帮我加个接口

高手更像这样说：

```text
目标：修复登录超时后用户无法重新认证的问题
范围：重点看 auth、session、token refresh 相关代码
约束：沿用现有 session 流程，不引入新库，不绕过错误处理
验证：先复现问题，写失败用例，修复后跑相关测试
```

你会发现，高手和普通用户的差距，首先不是“技术”，而是 **任务定义质量**。

### 小任务 vs 大任务
#### 小任务
例如：
- 改文案
- 修 typo
- 加一行日志

这类可以直接做。

#### 大任务
例如：
- 涉及多文件
- 涉及架构
- 你自己也不确定方案

这类先 plan。

### 高手的标准做法
```text
/plan 修复登录超时问题，先不要改代码，先分析现状并给出计划
```

或者直接说：

```text
先不要改代码。
先分析现状、根因和改动范围，给出实施计划和验证方式。
```

## 五、阶段 3：真正开始开发时，高手怎么驱动 Claude Code
这个阶段最容易浪费时间。

### 高手不会只说“去做”
他们通常会让 Claude 按顺序推进：

1. 先读相关文件
2. 再定位现状
3. 再实施改动
4. 再验证
5. 最后汇报改动和风险

### 一个高手常用模板
```text
先读相关文件并确认当前实现方式。
然后实施最小修改，不要重写。
改完后跑相关测试，并总结改动、剩余风险和是否需要补文档。
```

### 如果是前端改动
高手会多加两个约束：
- 参考已有页面模式
- 视觉改动要给可验证结果

例如：

```text
按现有设计系统和组件模式修改。
不要重新发明交互。
如果有截图对比条件，就用截图或可运行页面验证。
```

### 如果是后端改动
高手会强调：
- 保持接口兼容
- 不要吞错
- 不要用 mock 掩盖根因

例如：

```text
优先修根因，不要用 try/catch 吞掉错误来掩盖问题。
如果改动影响 API 行为，请说明兼容性影响。
```

## 六、阶段 4：遇到 bug 时，高手怎么用 Claude Code
修 bug 是高手最能拉开差距的地方。

### 普通用户常见问法
- 这里报错了，你看看

### 高手常见问法
```text
这里有一个可复现错误：...
先定位根因，不要拍脑袋改。
如果可能，先写失败用例复现问题，再修复，最后跑相关测试。
```

### 如果你已经知道怀疑范围
```text
问题出现在用户 session 超时后重新登录的流程。
重点检查 `src/auth/` 和 token refresh 相关逻辑。
先解释根因，再动手修。
```

### 高手修 bug 的节奏
1. 复现
2. 缩小范围
3. 找根因
4. 写失败用例
5. 修复
6. 回归验证

### 什么时候值得用 skill
如果你经常在做这种事情，就把这整套流程做成一个 `bugfix` skill。

因为这已经不是“一个需求”，而是你自己的稳定工作套路。

## 七、阶段 5：写功能写久了，高手会怎么处理中途失控
这是很多人最不会的一步。

一个功能改着改着，经常会出现：
- 上下文太长
- 方案半路变了
- Claude 开始急着收尾
- 旧错误思路反复出现

高手不会硬顶。

### 第一种处理：`/compact`
适合：
- 任务没变
- 只是上下文变长
- Claude 还没有明显跑偏

用法：

```text
/compact 只保留当前功能实现相关的目标、已完成内容、待做项和验证结果
```

### 第二种处理：`/clear` + 重开
适合：
- 已经明显跑偏
- 同一问题纠偏两次还不对
- 输出里混入很多旧上下文噪音

### 第三种处理：先写交接，再重开
这是高手最值钱的习惯之一。

让 Claude 先写一份交接：

```text
把当前任务的目标、已完成内容、未完成项、失败尝试、下一步建议整理成 markdown。
```

然后新开 session：

```text
读这个交接文件，接着做，不要重复已经确认失败的路径。
```

这一步非常像一个高手工程师在做真实项目交接。

## 八、阶段 6：重构时，高手怎么避免“重构变重写”
重构最容易被 Claude 做成重写。

所以高手一定会加这句话：

```text
按“行为不变”的前提小步重构。
沿用现有模式，不要重写，不要引入不必要的新抽象。
每一步都要可验证。
```

### 高手重构的典型流程
1. 先让 Claude 识别过时模式或重复逻辑
2. 再让它提出几个可行重构方案
3. 选一个最保守的方案
4. 分步骤实施
5. 每一步都跑验证

### 高手不会一上来就说
- 把这个模块重构一下

而更像是：

```text
找出这个模块里最值得改的三处重复或过时实现。
按“收益最大、风险最小”排序。
先改第一处，并验证行为不变。
```

## 九、阶段 7：测试阶段，高手不会只说“补测试”
高手会让 Claude 先找风险点，而不是只求覆盖率。

### 更好的问法
```text
找出这个模块里还没被覆盖、但最容易出问题的逻辑。
按项目现有测试风格补测试。
重点覆盖边界条件、错误路径和回归风险。
```

### 如果刚修完 bug
```text
围绕这个 bug 补一个回归测试。
要求它先失败，再因修复而通过。
```

### 如果是已有大模块
```text
先列出这个模块最缺测试的 5 个风险点，再决定补哪些测试。
不要先机械追覆盖率。
```

高手对测试的理解不是：
- “顺手补两条”

而是：
- “把验证闭环建出来”

## 十、阶段 8：评审阶段，高手会单独切换成 reviewer 模式
这是很多人不做，所以长期质量上不去的关键一步。

普通用户常常这样收尾：
- 改完了，看起来能跑，就结束

高手更常这样做：

```text
现在不要继续实现。
请切换到 code review 视角，只看风险、行为回归、边界条件、测试缺口和潜在副作用。
```

### 更专业一点的 review 提示
```text
按以下顺序 review 当前改动：
1. 行为回归
2. 错误处理
3. 测试缺口
4. 可维护性
5. 是否存在过度改动
```

### 如果改动较大
高手甚至会：
- 新开一个 session
- 只喂 diff 或相关文件
- 让 Claude 只做 review，不参与实现

这很重要，因为“写的人评自己”天然会放水。

## 十一、阶段 9：准备 commit / PR 时，高手会做什么
这个阶段不是让 Claude “写一段 PR 文案”就结束。

### 高手的收尾顺序通常是
1. 总结改动
2. 列出验证结果
3. 列出已知风险
4. 再生成 commit / PR 说明

### 常用提示
```text
先总结当前改动。
然后列出我实际做过的验证。
再列出剩余风险和回滚点。
最后写一个简洁的 commit message 和 PR 描述。
```

### 如果你用 GitHub
等你本地工作流成熟后，再考虑：
- `/install-github-app`
- GitHub Actions 里的 `@claude`
- `/autofix-pr`

但高手通常不是一开始就上这些自动化。  
他们会先把本地工作流跑顺。

## 十二、阶段 10：项目做完后，高手会怎么沉淀经验
这是高手最像“高手”的地方。

任务结束以后，普通用户就关掉窗口了。

高手会做一轮 very short retrospective：

### 1. 哪些纠偏重复出现了
如果一个纠偏你已经说了两次，它大概率该进入：
- `CLAUDE.md`
- `.claude/rules/`
- auto memory
- skill
- hook

### 2. 哪些流程已经稳定了
如果一个套路你用了三次以上，就开始考虑 skill 化。

典型候选：
- `bugfix`
- `review`
- `catchup`

### 3. 哪些动作不该再靠“记得去做”
如果一个动作每次都必须做，就考虑 hook。

例如：
- 提交前检查测试
- 修改敏感目录提醒
- 权限请求或空闲时通知

### 4. 哪些信息值得记住
如果这是：
- 所有人都该知道的长期事实 -> 项目 `CLAUDE.md`
- 你自己的长期偏好 -> 全局 `~/.claude/CLAUDE.md`
- 你重复纠正后希望 Claude 自己学会 -> 让它记进 auto memory

例如你可以直接说：

```text
remember that this project uses pnpm, not npm
```

或：

```text
remember that the API tests require local Redis before running
```

然后用 `/memory` 去看它是否真的写下来了。

## 十三、一个高手的一周，通常怎么用 Claude Code
如果你不是要一条功能，而是想形成习惯，可以把一周工作压成这样。

### 周一：新任务上手
- 用 Claude 建代码库地图
- 明确范围
- 补项目 memory

### 周二到周三：功能开发
- 先 plan
- 再实现
- 每轮都验证
- 途中 compact 或重开

### 周四：修 bug / 补测试 / 局部重构
- 用 `bugfix` 思路处理问题
- 补回归测试
- 做一次 review

### 周五：整理与沉淀
- 总结一周重复纠偏
- 写进 `CLAUDE.md` / skill / hook
- 跑 `/insights`
- 调整 permissions 或 settings

这才是真正像“高手工程师”一样在使用 Claude Code。

## 十四、如果你现在就想模仿高手，最值得立刻照做的 7 件事
1. 每个任务都先写“目标、范围、约束、验证”。
2. 每个新项目第一天都跑 `/init` 和 `/memory`。
3. 不要在一个脏 session 里死磕，学会 `/compact`、`/clear` 和交接重开。
4. 改动做完后，单独做一次 reviewer 视角的检查。
5. 重复三次以上的工作流，做成 skill。
6. 每次结束任务后，都想一遍“这次什么应该沉淀成长期资产”。
7. 先把本地工作流打磨好，再上 GitHub 自动化、远程 agent 和大规模并行。

## 十五、这一章真正想给你的，不是知识点，而是姿势
你现在最需要的，不是继续背更多命令，也不是再看一堆散文章。

你需要的是一个内化后的工作姿势：

- 项目开始前先搭规则
- 任务开始前先定边界
- 开发中持续验证
- 跑偏时及时重启
- 完成后单独 review
- 结束后沉淀经验

这套姿势一旦稳定下来，你就不再只是“会用 Claude Code”，而是在：

**像一个高手工程师那样，拿 Claude Code 真正做项目。**

## 十六、和前后章节的关系
- 第 10 章讲的是常见工作流模板
- 第 23 章讲的是高手技巧
- 第 24 章讲的是个人和团队工作实践
- 第 25 章讲的是单兵开发者如何追上高手
- 第 26 章讲的是：把这些东西真的串成一条完整项目流水线

## 学完标准
- 你知道一个项目从开始到交付，每个阶段 Claude Code 应该怎么用
- 你知道什么时候该写 memory，什么时候该做 skill，什么时候该上 hook
- 你知道高手不是一直聊天，而是在切换工作模式
- 你已经可以照着这一章，真的跟着做完一个项目

## 关键英文来源（截至 2026-04-25）
### 官方文档
- Common workflows  
  https://code.claude.com/docs/en/common-workflows
- Best Practices for Claude Code  
  https://code.claude.com/docs/en/best-practices
- How Claude remembers your project  
  https://code.claude.com/docs/en/memory
- Commands  
  https://code.claude.com/docs/en/commands
- Claude Code GitHub Actions  
  https://code.claude.com/docs/en/github-actions

### Anthropic 工程博客
- Harness design for long-running application development  
  https://www.anthropic.com/engineering/harness-design-long-running-apps
- Effective context engineering for AI agents  
  https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents
- Claude Code auto mode: a safer way to skip permissions  
  https://www.anthropic.com/engineering/claude-code-auto-mode

### 高质量实践者经验
- Shrivu Shankar, *How I Use Every Claude Code Feature*  
  https://blog.sshh.io/p/how-i-use-every-claude-code-feature
- Simon Willison, *How I Use Every Claude Code Feature*  
  https://simonwillison.net/2025/Nov/2/how-i-use-every-claude-code-feature/

### 我为什么采用这些来源
- 官方文档：提供项目全流程里的标准能力边界和工作流模板
- 工程博客：补上长任务、上下文和重启策略这些“高手最在意”的部分
- Shrivu / Simon：补上高手真实日常工作里的使用姿势，但只采用与官方不冲突、且被多人验证的经验
