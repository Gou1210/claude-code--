# 26. 从零到交付：一个项目全流程怎么用 Claude Code

## 重要程度
**S 级**。这一章不是功能介绍，而是一个项目从进入仓库到交付收尾的真实操作剧本。

如果你已经会一些基础命令，但总觉得自己只是“偶尔让 Claude 帮忙写代码”，还没有进入稳定项目节奏，这一章就是给你的。

## 学习目标
- 知道一个项目的每个阶段，Claude Code 应该扮演什么角色
- 知道什么时候该写 `CLAUDE.md`，什么时候该让它记忆，什么时候该做成 skill 或 hook
- 知道遇到长任务、跑偏、反复纠错时，该怎么止损和重开
- 学会一套你今天就能照做的标准工作流

## 先说结论：真正的差距不在命令，而在工作流设计
第 26 章上一个版本的问题，是把基础卫生和有区分度的做法混在了一起。那样没有用。

真正有用的版本，应该回答这些问题：

- 我刚进一个仓库时，第一小时该做什么
- 我接到一个需求时，第一条话该怎么说
- 我改代码改乱了，什么时候该 `compact`，什么时候该重开
- Claude 到底怎么“记住”东西，我该把规则放哪
- 一个任务做完后，什么该沉淀成项目记忆，什么该沉淀成 skill

你把这些问题解决了，Claude Code 才会从“会聊天的代码助手”变成“稳定的开发搭档”。

## 一、先建立一个正确认知：Claude Code 不是自动懂你，它只会使用你放进系统里的上下文
很多初学者最大的误区是：

- 以为说过一次，Claude 以后就应该记住
- 以为 skill 可以装所有规则
- 以为 `CLAUDE.md` 写越长越安全
- 以为一个 session 可以从头拖到尾

这些都不对。

你先记住下面这四条基本分工：

### 1. 项目事实，放进 `CLAUDE.md`
这里放长期稳定、几乎每次都值得知道的事实。

例如：
- 项目用 `pnpm`，不用 `npm`
- 测试前必须先起 Redis
- `apps/admin` 不能直接改共享组件
- 提交前至少要跑哪几个命令

### 2. 反复纠偏的个人或项目经验，放进 memory
Claude Code 的 memory 不是“自动永久大脑”，而是一个可管理的记忆层。

你可以明确告诉它：

```text
remember that this repo uses pnpm, not npm
```

或者：

```text
remember that API tests need local Redis before running
```

然后用：

```text
/memory
```

去检查它到底有没有记住。

要点只有一个：

**memory 不是你以为它记住了就算记住，必须检查。**

### 3. 可复用流程，放进 skill
skill 不是拿来塞项目规则的。

skill 适合放“步骤稳定、以后还会反复用”的流程。

例如：
- `bugfix`
- `review`
- `catchup`
- `trace-flow`

你以前把项目规则都放 skill 里，这就是很多初学者会犯的错。
因为 skill 的正确定位是“流程扩展”，不是“事实仓库”。

### 4. 不能靠自觉的动作，放进 hook
如果一件事你每次都得做，而且忘了代价很大，就不该继续靠自己记住。

例如：
- 提交前没跑测试就阻断
- 改到敏感目录就提醒
- 任务结束后发通知

这类动作最适合 hook。

## 二、项目刚开始的第一天，到底先做什么
更稳的开局不是“帮我实现 XXX”，而是先搭最小工作环境。

顺序通常是这样的：

### 第一步：跑 `/init`，但不要把生成结果当圣旨
如果这是新项目，先用：

```text
/init
```

它的价值不是“自动生成完美配置”，而是帮你把以下几类东西拉出来：

- 项目 `CLAUDE.md`
- 记忆与规则入口
- skills / hooks 的讨论起点

但成熟的用法不会生成完就结束，而是马上审查：

- 它写进 `CLAUDE.md` 的是不是长期事实
- 有没有把一堆长流程错误地塞进去
- 有没有遗漏 build / test / lint / run 这些基础命令

### 第二步：确认记忆层到底加载了什么
很多人其实连 Claude 现在加载了哪些记忆都没搞清楚。

先看：

```text
/memory
```

你至少要确认三件事：

- 当前项目的 `CLAUDE.md` 是否被加载
- 自动记忆是否开启
- 已经存在的记忆里有没有过时或错误内容

如果你这一步不做，后面经常会出现一种错觉：

“我明明说过了，它怎么还不知道？”

答案往往不是它笨，而是你根本没有把那条信息放进一个会被稳定加载的地方。

### 第三步：先让 Claude 帮你建仓库地图，而不是立刻改代码
第一小时最值得做的，不是实现需求，而是让 Claude 帮你回答：

- 入口在哪
- 主流程怎么走
- 哪些目录危险
- 测试怎么跑
- 哪些命令是日常必用

可直接这样说：

```text
先不要改代码。
请先给我这个仓库的工作地图：
1. 目录结构和关键入口
2. 主流程和核心模块
3. 构建、运行、测试方式
4. 哪些目录或文件改动风险最高
5. 如果我要改 XXX，应该先读哪些文件
```

### 第四步：让 Claude 帮你起草第一版项目 `CLAUDE.md`
这一步对初学者特别重要，因为你自己常常还不知道哪些东西值得记。

可以直接这样说：

```text
基于刚才的仓库分析，帮我起草一个项目 CLAUDE.md。
只写长期稳定、几乎每次都值得知道的事实。
不要写长流程，不要写临时任务，不要写今天这个需求。
```

如果它写出来的是一堆“做事步骤”，删掉。
项目 `CLAUDE.md` 应该像“项目常识卡”，不是“操作手册”。

## 三、接到一个新需求时，第一条话怎么说
质量差距，第一步就拉开了。

普通用户常说：

- 帮我加个功能
- 帮我看这个 bug
- 改一下这个页面

更稳的说法是：

```text
目标：支持用户在超时后重新登录
范围：auth、session、token refresh 相关代码
约束：沿用现有认证流程，不引入新库，不改公开 API
验证：先复现问题，再修复，再跑相关测试
先不要写代码，先分析现状、根因和实施计划
```

这五个字段你必须养成习惯：

1. 目标
2. 范围
3. 约束
4. 验证
5. 先分析还是先实现

你不给，Claude 就会自己猜。
它一猜，质量就不稳。

## 四、什么时候可以直接做，什么时候必须先 plan
很多人不是不会用 Claude，而是不知道什么时候该停下来先做计划。

可以用这个判断法：

### 直接做
满足下面大部分条件，就可以直接做：

- 只改 1 到 3 个文件
- 不涉及架构变化
- 你自己知道改哪
- 风险低
- 验证简单

### 先 plan
满足下面任意两条，就先 plan：

- 会跨多个模块
- 你自己也不确定根因
- 涉及数据库、权限、认证、支付、缓存
- 可能引入行为变化
- 改动后验证成本高

标准话术：

```text
先不要改代码。
先分析现状、根因、改动范围、风险点和验证方式。
给我一个分步骤计划，再等我确认。
```

## 五、开始实现时，怎么避免 Claude 一上来就重写
这是初学者最常见的坑之一。

你如果只说“实现这个功能”，Claude 很容易：

- 一次改太多
- 为了“看起来更优雅”顺手重构
- 引入新抽象
- 把局部改动做成全局改造

更稳的做法是在开工前加这几句：

```text
先读相关文件，确认当前实现模式。
按最小改动实现，不要重写，不要顺手重构。
优先沿用现有模式。
改完后跑相关验证，并总结改动、风险和后续建议。
```

如果你是前端，再加：

```text
参考现有页面和组件模式，不要重新发明交互。
```

如果你是后端，再加：

```text
优先修根因，不要通过吞错或绕过流程掩盖问题。
```

## 六、修 bug 时，不要让 Claude 猜，而是让它走调查流程
修 bug 的标准流程，不是“看看这里为什么报错”，而是：

1. 复现
2. 缩小范围
3. 找根因
4. 写失败用例
5. 修复
6. 回归验证

你可以直接这样说：

```text
这里有一个可复现问题：...
先不要拍脑袋改。
先定位根因，尽量给出最小复现路径。
如果合适，先补一个失败测试，再修复，再跑相关测试。
```

如果你已经知道怀疑区域：

```text
重点检查 src/auth 和 token refresh 相关逻辑。
先解释根因，再实施修复。
```

这里有一个真正有区分度的点：

**如果 Claude 还没解释清根因，就不要让它开始改。**

## 七、写测试时，重点是风险，不是覆盖率数字
初学者常说：

- 补点测试

更稳的说法是：

```text
先找出这次改动最容易回归的路径。
按现有测试风格补测试，重点覆盖边界条件、错误路径和这次 bug 的回归场景。
不要机械追覆盖率。
```

如果是修 bug 后补测试，最好的说法是：

```text
围绕这次 bug 写一个回归测试。
要求修复前会失败，修复后通过。
```

## 八、review 为什么最好和实现分开
这不是“技巧”，而是很多成熟使用者默认会做、但官方文档不会天天替你强调的工作习惯。

原因不是玄学，而是上下文污染：

- 实现上下文里已经塞满了你的思路、假设和妥协
- Claude 刚替你写完代码时，会天然延续同一条思路
- 同一个上下文里做 review，它更容易替自己找理由

所以真正稳的做法是：

### 方案 1：中小改动，用同一 session 强制切 reviewer 视角

```text
停止实现。
现在只做 review，不提供新方案。
只检查：
1. 行为回归
2. 错误处理
3. 测试缺口
4. 过度改动
5. 可维护性风险
```

### 方案 2：中大改动，新开 session 做 review
这是更接近成熟团队默认做法的方案。

原因不是“仪式感更强”，而是：

- 新 session 没有实现时的自我辩护
- 你可以只喂 diff、关键文件和任务目标
- review 的注意力更集中，不会继续顺着实现思路往下写

如果你将来只学会一个真正有区分度的习惯，我反而建议是这个，而不是 `/compact`。

## 九、真正有区分度的，不是“别死磕脏 session”，而是 session 拓扑怎么设计
你刚才指出得对。
“不要在一个脏 session 里死磕”只是常识，不配叫进阶知识。

真正能拉开差距的是：

### 1. 什么时候该继续当前 session
适合继续当前 session 的情况：

- 还是同一个小任务
- 改动范围稳定
- 你主要需要它延续刚才的文件上下文
- 没有明显跑偏

### 2. 什么时候该新开一个 session
适合新开的情况：

- 要切任务
- 要从实现切到 review
- 要从修 bug 切到补测试
- 要重新审视方案，而不是延续刚才的判断

真正的差距不是“更会 `/clear`”，而是更早知道：

**当任务类型变了，就该让上下文也跟着变。**

### 3. 什么时候该直接用 worktree，而不是继续在同一个工作区聊
这才是很多初学者没意识到的点。

如果你要并行处理下面这些事，worktree 通常比一个 session 更值钱：

- 一个分支在做功能，另一个分支在 hotfix
- 你想让一个 Claude 实现，另一个 Claude review
- 你想比较两个不同方案
- 你不想让新任务污染当前改动现场

对单兵开发者来说，worktree 的价值往往比 subagent 更高。
因为它解决的是更现实的问题：**隔离上下文、隔离分支、隔离实验。**

### 4. 什么时候应该先写 handoff，而不是继续聊
只有这几种情况值得写 handoff：

- 任务今天做不完
- 你已经试过两条失败路径
- 你准备明天再接
- 你准备换一个全新 session 或 worktree 接手

真正有价值的 handoff 不是流水账，而是这 6 项：

1. 目标
2. 当前结论
3. 已完成内容
4. 已验证结果
5. 已确认失败的路径
6. 下一步最优先动作

## 十、一个任务做完后，让 Claude 帮你做“资产分类”
这才是你前面真正想问，但中文互联网几乎不讲清楚的东西。

大多数人任务做完就结束。
成熟用法会多做一步：

**让 Claude 帮你判断，这次任务里有哪些东西该沉淀到哪一层。**

直接这样说：

```text
基于刚才这次任务，请把值得沉淀的内容分成四类：
1. 应写入项目 CLAUDE.md 的长期事实
2. 应写入 memory 的纠偏经验
3. 应做成 skill 的可复用流程
4. 应做成 hook 的强制检查
请分别给出建议，并说明原因。
```

这一步很关键，因为它把 Claude 从“当前任务执行者”变成“你的工作流设计助手”。

### 这四类到底怎么区分
#### 进项目 `CLAUDE.md`
特征：

- 长期稳定
- 项目级
- 几乎每次都值得知道

例如：
- 必须先起哪些依赖
- build / test / lint 命令
- 哪些目录不能乱改

#### 进 memory
特征：

- 不是说明书，但以后少错一次就值
- 更像经验和纠偏
- 未必值得永久写死在 `CLAUDE.md`

例如：
- 这个仓库里排查认证问题先看哪条日志
- 这个项目的 review 更关注兼容性而不是重构优雅

#### 进 skill
特征：

- 是流程，不是事实
- 重复 3 次以上
- 以后几乎还会再用

例如：
- `bugfix`
- `review`
- `catchup`

#### 进 hook
特征：

- 不能靠记忆
- 忘了代价大
- 适合在固定节点提醒或阻断

例如：
- commit 前校验
- 改某个目录时提醒
- 长任务结束时通知

## 十一、Claude 到底怎么“主动记忆”和“被动记忆”
这不是术语问题，而是实操问题。

### 1. 所谓“被动记忆”，本质上是稳定加载层
真正接近“被动记忆”的，不是聊天记录本身，而是：

- 项目 `CLAUDE.md`
- 个人 `CLAUDE.md`
- 已保存并会被加载的 memory

所以你别再把“我说过一次”理解成“它有被动记忆”。
说过一次，通常只是聊天记录，不是系统化记忆。

### 2. 最稳的做法，是让 Claude 先判断，再由你决定写到哪
很多人一上来就问：

- 这个是不是该记住？

更专业的问法是：

```text
这条信息更适合进入项目 CLAUDE.md、memory、skill 还是 hook？
请给出分类和理由。
```

这比你自己瞎猜稳定得多。

### 3. 怎么让 Claude 形成“主动帮你沉淀”的习惯
不是靠它自己悟，而是靠你给它固定收尾动作。

每次任务结束都加一句：

```text
现在做收尾分类：
哪些内容值得沉淀，分别进 CLAUDE.md、memory、skill、hook？
```

你连着做十次，Claude 在这个项目里的行为就会越来越像“会帮你整理资产的搭档”，而不是一次性工具。

这才接近你说的“主动生成记忆”。

## 十二、真正有区分度的 8 个非显眼常识
下面这些，才更像英文世界成熟使用者默认知道、但中文新手常常没系统学过的东西。

### 1. `CLAUDE.md` 不是越长越好，而是越像项目常识卡越好
里面应该是长期事实，不是长篇操作流程。
长流程应该挪去 skill。

### 2. 先让 Claude 帮你起草 `CLAUDE.md`，再亲手删废话
初学者自己写，常常不知道哪些值得记。
更稳的做法是让 Claude 先提一版，再人工压缩。

### 3. 每个任务结束都做一次“资产分类”
这一步比很多命令都值钱，因为它会持续提升你整个项目的使用质量。

### 4. review 最好和 implementation 分离
不是为了形式，而是为了减少上下文偏见。

### 5. 对单兵开发者来说，worktree 的杠杆常常高于 subagent
因为你更常遇到的是并行任务隔离，而不是复杂代理编排。

### 6. Claude 更适合先帮你找“风险最大处”，再去写测试
成熟使用者不会让它为了覆盖率到处补无效测试。

### 7. 不是所有经验都该写死进 `CLAUDE.md`
很多只是临时纠偏，更适合进 memory。
写死太多，反而会让真正重要的信息被淹掉。

### 8. 真正的差距来自稳定流程，不来自神 prompt
你看到别人很顺，通常不是因为他会一句神奇咒语，而是他的项目已经分好了：

- 事实层
- 记忆层
- 流程层
- 强制校验层

## 十三、一个项目的标准工作流，照这个节奏跑
如果你真想完整做一遍项目，请照下面的节奏跑。

### 阶段 0：新项目第一天
你做：

1. `/init`
2. `/memory`
3. 让 Claude 画仓库地图
4. 让 Claude 起草项目 `CLAUDE.md`
5. 你手工删掉流程废话，只保留长期事实

### 阶段 1：接需求
你做：

1. 写清目标、范围、约束、验证
2. 判断是直接做还是先 plan
3. 如果是中大任务，先分析不动手

### 阶段 2：实现
你做：

1. 先读相关文件
2. 明确要求最小改动
3. 改完跑验证
4. 总结改动和风险

### 阶段 3：修 bug
你做：

1. 先复现
2. 再解释根因
3. 再修
4. 补回归测试

### 阶段 4：review
你做：

1. 切换或新开 reviewer session
2. 只查风险、回归、缺口和副作用

### 阶段 5：收尾资产化
你做：

1. 让 Claude 分类哪些进 `CLAUDE.md`
2. 哪些进 memory
3. 哪些做成 skill
4. 哪些做成 hook

这最后一步，往往才是稳定用法和一次性用法真正拉开差距的地方。

## 十四、初学者最容易犯的 10 个错误
1. 把 skill 当成项目规则仓库。
2. 把所有东西都塞进一个超长 `CLAUDE.md`。
3. 以为说过一次，Claude 以后就会自动记住。
4. 新需求一来就直接让它写代码。
5. 修 bug 时没讲清复现路径和怀疑范围。
6. 改代码时不强调“最小改动”，结果被它顺手重构。
7. 把实现、review、补测试都放在一个 session 里。
8. 该用 worktree 隔离任务时，硬在一个工作区里混做。
9. 只让它“补测试”，不说要覆盖什么风险。
10. 任务结束就关窗口，不做资产分类。

## 十五、如果你今天只能开始做 5 件真正有杠杆的事
1. 让 Claude 帮你起草项目 `CLAUDE.md`，然后亲手删到只剩长期事实。
2. 每个任务结束都做一次“沉淀分类”，别再只做当前任务。
3. 把 review 从实现上下文里分离出来。
4. 遇到并行任务时优先考虑 worktree，而不是继续污染当前 session。
5. 把你重复三次以上的流程做成 skill，把忘一次就出事的动作做成 hook。

## 十六、这一章真正想给你的东西
不是“有人知道更多命令”。
而是：

**真正重要的是：你知道一个项目里，哪些信息该进哪一层，哪些任务该换上下文，哪些经验该沉淀成长期资产。**

这才是你现在真正缺的，不是更多口号。

## 十七、和前后章节的关系
- 第 23 章讲技巧
- 第 24 章讲团队与工作实践
- 第 25 章讲单兵开发者如何追上成熟使用者的工作节奏
- 第 26 章讲一个项目从开始到结束的标准剧本
- 第 27、28 章再分别展开进阶常识与个人配置

## 学完标准
- 你知道一个项目从进入仓库到收尾，每个阶段该怎么驱动 Claude
- 你知道 `CLAUDE.md`、memory、skill、hook 的分工
- 你知道长任务跑偏时该怎么止损
- 你已经可以按这一章的顺序，真的完成一次项目开发

## 关键英文来源（截至 2026-04-25）
### 官方文档
- Best practices for Claude Code
  https://code.claude.com/docs/en/best-practices
- Common workflows
  https://code.claude.com/docs/en/common-workflows
- How Claude remembers your project
  https://code.claude.com/docs/en/memory
- Skills
  https://code.claude.com/docs/en/skills
- Hooks
  https://code.claude.com/docs/en/hooks
- Commands
  https://code.claude.com/docs/en/commands

### Anthropic 工程博客
- Effective context engineering for AI agents
  https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents
- Harness design for long-running application development
  https://www.anthropic.com/engineering/harness-design-long-running-apps
- Claude Code auto mode: a safer way to skip permissions
  https://www.anthropic.com/engineering/claude-code-auto-mode

### 高质量实践者经验
- Shrivu Shankar, *How I Use Every Claude Code Feature*
  https://blog.sshh.io/p/how-i-use-every-claude-code-feature
- Simon Willison, Claude Code 相关长期观察
  https://simonwillison.net/tags/claude-code/

## 这些结论分别来自哪里
- `CLAUDE.md` / memory / skills / hooks 的正式能力边界：主要来自官方文档
- 长任务、交接、上下文压缩、验证闭环：主要来自 Anthropic 工程博客
- review 分离、最小改动、不要过度依赖单个长 session、把流程沉淀成资产：主要来自英文实践者共识，尤其是 Shrivu 与 Simon 长期经验
