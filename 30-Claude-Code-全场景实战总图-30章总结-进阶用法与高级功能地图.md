# 30. Claude Code 全场景实战总图：30 章总结、进阶用法与高级功能地图

## 重要程度
**S+ 级**。这不是第 1 到 29 章的压缩版目录，也不是基础功能回顾。

这篇文章要解决的是更实际的问题：

- 真实开发里，遇到一个场景，你该先开什么模式
- Claude Code 的高级能力在什么边界下最值得用
- 规则到底写进 `CLAUDE.md`、memory、skill、hook、settings 的哪一层
- 高级功能到底什么时候能提效，什么时候反而会让结果变差
- 你怎样把 Claude Code 从“偶尔帮你写几段代码”用成“稳定的开发系统”

如果你已经用了一个月 Claude Code，这一章应该成为你的总地图。

## 这篇文章怎么读
这 30 章不是按“功能说明书”写的，而是按真实开发工作顺序写的。

你可以这样用：

- 刚接新仓库：看第 1 到 6 章
- 要做需求、修 bug、重构、补测试：看第 7 到 14 章
- 要进入并行、多代理、自动化：看第 15 到 22 章
- 要做长任务、团队协作、系统化沉淀：看第 23 到 30 章

## 先给结论：真正的高手差距，主要来自这 6 件事
不是更多命令，不是更长 prompt，也不是装更多扩展。

真正能稳定拉开差距的，通常是：

1. 会先定义任务壳，而不是直接让 Claude 开写
2. 会把问题表达成可执行边界，而不是模糊地下指令
3. 会把实现、验证、审查拆开，而不是混成一锅
4. 会把规则、流程、强制检查分层沉淀，而不是全塞进一个地方
5. 会设计恢复点、交接物和工作区隔离，而不是任务乱了再补救
6. 会把 Claude Code 接入项目系统，而不是只把它当聊天窗口

## 一、Claude Code 真正有价值的，不是“定位”，而是 4 种可复用执行单元
前面那种“不是聊天框，是执行系统”的写法太虚，没什么信息量。真正值得记的是：你在真实开发里，反复调用的其实只有 4 种执行单元。

### 1. Explore
用途：

- 找代码
- 追链路
- 对齐事实
- 缩小问题范围

适合指令：

```text
先不要改代码。
只做调查：
1. 找出和登录失败相关的入口、服务和状态管理文件
2. 从用户点击登录一路追到 token 刷新
3. 标出最可能的 3 个根因点
4. 每个根因都给出对应证据，不要猜
```

### 2. Plan
用途：

- 需求还没定型
- 改动面大
- 你要比较方案
适合指令：

```text
先不要实现。
基于当前仓库，给出这个需求的 2 到 3 种可行方案。
每个方案都写清：
1. 改哪些文件
2. 风险是什么
3. 怎么验证
4. 哪个方案最稳，为什么
```

### 3. Implement
用途：

- 已经有明确目标和边界
- 允许进入编码与验证

适合指令：

```text
按方案 B 实现。
要求：
1. 保持现有接口契约不变
2. 先补失败用例再修
3. 改完后跑相关测试
4. 最后只汇报根因、改动点、验证结果、剩余风险
```

### 4. Review
用途：

- 独立审查
- 找遗漏
- 找回归风险

适合指令：

```text
不要复述改动。
把自己当 reviewer，只找问题：
1. 是否有行为回归风险
2. 是否遗漏测试
3. 是否破坏边界或契约
4. 如果这是 PR，你会卡在哪几条意见上
```

你真正该熟练的不是“Claude 的定位”，而是这 4 个执行单元怎么切，什么时候单独用，什么时候串起来用。

## 二、任务类型真正有用的写法，不是分类本身，而是“对应起手模板”
任务分类如果不落到具体操作，确实就是废话。真正有用的是：不同任务，第一轮该怎么开。

### 1. 明确小任务：直接进实现，但把验证钉死
适用：

- 改动点明确
- 文件范围小
- 你知道结果长什么样

直接这样开：

```text
修改 `src/auth/session.ts`，修复 session 超时后重新登录失败的问题。
约束：
1. 只改 auth 相关文件
2. 不改接口返回结构
3. 不要吞错
验证：
1. 新增或修改一个能复现问题的测试
2. 运行相关测试
3. 汇报根因和验证结果
```

### 2. 模糊需求任务：先 plan，输出方案对比表
适用：

- 你知道要什么结果，但不知道实现路径
- 可能牵涉多个模块

直接这样开：

```text
我要给后台加一个“批量冻结用户”能力。
先不要实现，先基于当前仓库给方案对比表。
至少比较：
1. 前后端接口设计
2. 权限校验放在哪
3. 失败回滚怎么做
4. 哪种方案对现有代码侵入最小
```

输出里至少要逼它说清：

- 改动文件范围
- 风险点
- 验证方式
- 推荐方案

### 3. 调查型任务：把输出格式钉成“证据链”
适用：

- 查链路
- 查根因
- 看日志
- 理解陌生模块

直接这样开：

```text
调查用户注册后没有收到欢迎邮件的问题。
先不要改代码。
输出格式固定为：
1. 相关文件列表
2. 实际调用链
3. 可能失败点
4. 每个失败点对应证据
5. 你还缺什么信息才能继续判断
```

调查型任务里，最有用的不是“分类”两个字，而是你有没有把它锁成证据链输出。

### 4. 高风险任务：把禁止事项和回滚点先说死
适用：

- 权限
- 支付
- 认证
- 数据迁移
- 删除逻辑

直接这样开：

```text
实现这次权限模型调整，但先按高风险任务处理。
要求：
1. 先列出受影响的入口、权限判断点、数据模型
2. 不允许直接删除旧逻辑，先兼容
3. 明确写出回滚方案
4. 明确哪些测试必须通过
5. 实现后再做一轮 reviewer 视角的风险审查
```

### 5. 长任务：不要一句“分 sprint”带过，直接按这个骨架执行
长任务真正需要的是一份 `sprint contract`。

一个最小可用的 `sprint contract` 至少写这 6 项：

1. `Goal`：这一轮具体交付什么
2. `Non-goals`：这一轮明确不做什么
3. `Files`：本轮允许动哪些目录或文件
4. `Checks`：这一轮怎么验证
5. `Artifacts`：结束时必须留下什么交接物
6. `Rollback`：失败了回到哪里

你可以直接这么开：

```text
这个需求不要一口气做完，先只做 sprint 1。

sprint 1 contract:
Goal:
- 跑通最小登录链路，先让新 token 流程工作

Non-goals:
- 不做 UI 美化
- 不做历史逻辑删除
- 不做二期监控埋点

Files:
- 只允许改 `src/auth/`、`src/api/auth.ts`、相关测试文件

Checks:
- 登录成功测试通过
- token 过期后刷新测试通过
- 旧接口兼容测试通过

Artifacts:
- 根因摘要
- 改动文件列表
- 还没做的事项
- sprint 2 建议

Rollback:
- 保留旧 token 流程开关，失败时能切回
```

这才叫“分 sprint”。不是一句口号，而是先把阶段边界和交付物写死。

## 三、刚接手陌生仓库时，第一轮怎么开才不浪费时间
这一节如果不落到具体话术，也是在讲空话。真正可用的是固定成 3 轮。

### 第 1 轮：拿仓库地图

```text
先不要改代码。
用最短篇幅告诉我这 5 件事：
1. 目录结构里最核心的 5 个目录分别负责什么
2. 应用入口在哪里
3. 主流程从哪里开始
4. 测试怎么跑
5. 哪些目录是高风险改动区
```

### 第 2 轮：拿任务相关链路

```text
现在只看和“登录”相关的链路。
输出格式固定为：
1. 入口文件
2. 中间调用链
3. 状态或数据落点
4. 相关测试文件
5. 你认为最容易误改的 3 个点
```

### 第 3 轮：拿改动前的影响面

```text
如果我要改这条登录链路，先不要改。
只回答：
1. 最小改动面在哪
2. 会波及哪些共享模块
3. 最低限度需要补哪些验证
```

这 3 轮跑完再开始实现，才算真正进入仓库。

## 四、任务壳怎么写：直接套这个 5 段模板
不要说“帮我做一下”。直接按下面 5 段写。

```text
任务：
修复 session 超时后重新登录失败的问题。

范围：
只看 `src/auth/`、`src/session/`、相关测试文件。

约束：
1. 不改接口返回结构
2. 不吞错
3. 不顺手重构无关模块

验证：
1. 先补一个能复现问题的测试
2. 跑 auth 相关测试
3. 如果有 e2e，就说明这轮是否运行

交付：
最后只输出：
1. 根因
2. 改动文件
3. 验证结果
4. 剩余风险
```

### 这个模板为什么够用
- `任务` 决定目标
- `范围` 防止乱看乱改
- `约束` 防止顺手改飞
- `验证` 防止只交代码不交结果
- `交付` 防止最后说一堆废话

## 五、上下文不是讲道理，直接给 Claude 一份“输入包”
比起讨论长度，更有用的是你每次给它的输入包长什么样。

### 可直接复用的输入包模板

```text
Current goal:
修复 session 超时后重新登录失败的问题。

Confirmed facts:
- refresh 接口返回 200
- 前端状态没有更新
- 已有失败测试可以复现

Scope:
- `src/auth/`
- `src/session/`
- `tests/auth/`

Constraints:
- 不改接口契约
- 不吞错
- 不做无关重构

Unknowns:
- 移动端入口是否复用同一 store

Required output:
- 根因
- 修复方案
- 验证结果
- 剩余风险
```

你以后不需要争论上下文该多长，先看你有没有把输入整理成这种结构。

## 六、任务结束后，怎么判断内容该沉淀到哪一层
不要背定义，直接用这段分类提示。

```text
基于刚才这次任务，把值得沉淀的内容分成 5 类：
1. 应进入 `CLAUDE.md` 的长期事实
2. 应进入 memory 的纠偏经验
3. 应做成 skill 的流程
4. 应做成 hook 的强制检查
5. 应写进 settings / permissions 的边界

每一类都按这个格式输出：
- 内容
- 为什么放这里
- 示例写法
```

### 一个最常见的判断例子
- `项目必须用 pnpm`：进 `CLAUDE.md`
- `这个仓库 review 时优先看兼容性，不鼓励顺手重构`：进 memory
- `每次修 bug 都先复现再修再 review`：做成 skill
- `提交前必须跑 auth 相关测试`：做成 hook 或 CI
- `禁止默认执行删除类命令`：进 settings / permissions

## 七、什么时候该先 plan：不是讲原则，是看你要它产出什么
如果一节讲 `plan`，就必须回答两件事：什么时候开，开了之后要它吐什么。

### 适合先 plan 的任务
- 你需要比较 2 个以上方案
- 这次改动跨前后端或跨多个模块
- 你必须保持兼容
- 你不确定验证闭环

### plan 阶段的直接提示

```text
先不要实现，只做 plan。
基于当前仓库，输出一份实施计划，必须包含：
1. 目标和非目标
2. 可能的 2 个方案
3. 每个方案要改哪些文件
4. 每个方案的风险
5. 推荐方案和理由
6. 最低验证清单
7. 哪些地方信息还不够，需要先调查
```

### plan 输出不合格的判断标准
- 只有抽象建议，没有文件范围
- 只有“风险较高”，没有具体风险点
- 只说“需要测试”，没说测什么
- 没有推荐方案
- 没有列出信息缺口

只有 plan 输出过这一关，才值得进入实现。

## 八、session 真正影响结果的，不是“长短”，而是你有没有换任务身份
这里不讨论习惯，只说什么时候换 session 会直接影响产出。

### 该换 session 的 4 个实操场景

#### 1. 从实现切到 review
直接这样开：

```text
下面不要继续实现。
把当前改动当成别人提交的 PR，按 reviewer 视角重新检查。
重点只看：
1. 回归风险
2. 漏掉的测试
3. 契约破坏
```

#### 2. 从调查切到修复
直接这样开：

```text
前面的调查先收束。
现在只基于已确认事实给修复方案，不要再继续扩大搜索范围。
```

#### 3. 之前已经在错误假设上跑了很多轮
直接这样开：

```text
忽略前面未经证实的推测。
只基于下面确认过的事实重新判断：
1. ...
2. ...
3. ...
```

#### 4. 你要故意获得一个独立视角
直接这样开：

```text
把下面内容当成新的任务说明，不参考前面历史对话的结论。
只根据这份输入重新判断最稳方案。
```

真正重要的不是“换不换”，而是你是否意识到当前任务身份已经变了。

## 九、`clear`、`compact`、`new session`、`worktree` 分别怎么用
这一节不谈概念，只给最短使用建议。

### `clear`
用途：
- 只是清屏，不解决判断问题

### `compact`
适合：
- 当前任务没变
- 但你想压缩历史噪音

压缩前最好先让 Claude 生成一份状态摘要：

```text
先把当前任务压缩成一份状态摘要，只保留：
1. 目标
2. 已确认事实
3. 已完成改动
4. 未解决问题
5. 下一步
```

### `new session`
适合：
- 你要切换任务身份
- 你想故意摆脱旧假设

### `worktree`
适合：
- 你连代码工作目录都要隔离
- 你要并行做稳定版和实验版

如果一句话总结：
- 只清屏，用 `clear`
- 只压缩历史，用 `compact`
- 要换判断容器，用 `new session`
- 要换代码空间，用 `worktree`

## 十、看代码链路时，不要问“在哪”，要让它按固定格式追
直接用下面这个模板追一条链路。

```text
追踪“用户点击登录”这条链路。
输出格式固定为：
1. Entry points
2. Main call path
3. State changes
4. External I/O
5. Data write points
6. Related tests
7. Most likely modification points
```

### 如果你要追前后端一整条链

```text
从前端点击登录开始，追到后端接口、数据库写入或 session 状态变化为止。
要求：
1. 每一步都给对应文件
2. 不要漏掉中间 service / store / middleware
3. 最后指出最小改动点
```

这种固定格式比“讲讲登录流程”有用得多。

## 十一、修 bug 的实操模板：按“复现 -> 证据 -> 修复 -> 回归”四段走
直接用下面这个模板，不要让它一上来就改。

```text
修这个 bug，但严格按四段执行：

第 1 段：复现
- 先告诉我怎么稳定复现
- 如果没有测试，先补一个最小失败用例

第 2 段：证据
- 只列出已确认事实
- 给出最可能的 1 到 2 个根因
- 每个根因都要对应证据

第 3 段：修复
- 只实现最小修复
- 不做顺手重构

第 4 段：回归
- 重新运行失败用例
- 说明还需要哪些相关回归测试
```

### bug 修复最常见的 4 个禁止项
- 没复现就改
- 一次改多个可疑点
- 用吞错掩盖问题
- 只汇报“已修复”，不汇报验证结果

## 十二、重构怎么开，才能避免它把“重构”做成“重写”
直接这样开：

```text
对这个模块做重构，但严格满足下面条件：
1. 行为不变
2. 接口契约不变
3. 不引入新依赖
4. 分 2 到 4 个小步提交思路
5. 每一步都说明怎么验证
6. 如果发现必须改契约，先停下来说明原因
```

### 如果你要它先给重构计划

```text
先不要改代码。
先列出这个模块里最值得重构的 3 个点。
每个点都写：
1. 当前问题
2. 最小改法
3. 风险
4. 如何验证行为不变
```

## 十三、补测试时，先让它列“场景表”，再让它写
不要直接说“补测试”。先让它吐一个测试场景表。

```text
先不要写测试代码。
先列出这个功能最值得补的测试场景表，按下面格式：
1. 场景名
2. 为什么要测
3. 输入
4. 预期输出
5. 属于正常路径、边界、异常还是回归
```

确认场景表后，再下第二轮：

```text
现在只补这 3 个场景的最小有效测试：
1. ...
2. ...
3. ...

要求：
- 不要和实现细节强绑定
- 断言只围绕行为结果
- 写完后说明这些测试挡住了什么回归
```

## 十四、review 怎么做才不是“再总结一遍”
实现后，直接切到这个审查模板：

```text
现在不要再实现，把当前改动当成一份别人提交的 PR。
只做 review，输出格式固定为：
1. Severity
2. Problem
3. Trigger
4. Impact
5. Missing test or evidence
```

### 如果没有发现问题
也不要让它写一堆表扬话，直接要求：

```text
如果没发现问题，只回答：
1. No major findings
2. Residual risks
3. Tests not run
```

## 十五、subagent 真正怎么分配任务
如果你写“适合调查、适合隔离”，还是空。实操上最常见的是下面 3 种委派。

### 委派 1：链路调查 agent

```text
你只负责调查认证链路。
不要改代码，只输出：
1. 相关文件
2. 调用链
3. 最可能的 3 个风险点
4. 证据
```

### 委派 2：测试执行 agent

```text
你只负责跑相关测试并整理失败摘要。
不要提修复方案，只输出：
1. 跑了哪些测试
2. 哪些失败
3. 关键错误信息
4. 可能关联的文件
```

### 委派 3：security / reviewer agent

```text
你只从安全和边界视角审查这次改动。
不要复述实现。
只输出高风险问题、触发条件和影响范围。
```

### 不要委派的情况
- 你还没定义清楚任务
- 当前就需要主会话拍板
- 需要强依赖所有上下文做综合设计

## 十六、多代理协作时，角色拆分直接照这个最小流水线
最小可用流水线就 4 个角色，不要再虚构复杂组织。

### 1. explorer
交付：
- 文件列表
- 调用链
- 风险点

### 2. planner
交付：
- 推荐方案
- 文件范围
- 验证清单

### 3. implementer
交付：
- 代码改动
- 测试结果
- 剩余风险

### 4. reviewer
交付：
- findings
- 缺失验证
- 是否建议合并

### 多代理最容易翻车的地方
- explorer 开始给修复方案
- implementer 顺手改计划
- reviewer 变成改动复读机
- 没有统一 handoff artifact

## 十七、worktree 到底怎么用：不是概念，是三个具体打法
如果只说“worktree 很重要”，那还是空话。真正有用的是你知道什么时候开，怎么开，开出来干嘛。

### 打法 1：实现和 review 分离
场景：

- 你已经改完一版
- 想用更干净的环境做 review

做法：

```powershell
git worktree add ..\repo-review -b review-auth-fix
```

然后在新目录里只做两件事：

- 看 diff
- 跑验证

不要在 review worktree 里继续顺手重构，不然“审查分离”立刻失效。

### 打法 2：稳定修复和激进实验分离
场景：

- 你有一个稳妥修法
- 同时又想试一个更大改法

做法：

```powershell
git worktree add ..\repo-safe -b fix-auth-safe
git worktree add ..\repo-exp -b fix-auth-experiment
```

然后：

- `repo-safe` 只做最小修复
- `repo-exp` 才允许改结构

这样你不会因为实验分支搞乱当前可交付版本。

### 打法 3：长任务按 sprint 分工作区
场景：

- 一个任务要做几天
- 每个阶段的改动性质不同

做法：

- `repo-s1`：只做最小可运行链路
- `repo-s2`：补兼容和清理
- `repo-s3`：补监控、文档、收尾

这样每个 sprint 的工作空间、上下文和产出天然隔离。

### worktree 最少该记住的命令

```powershell
git worktree list
git worktree add ..\repo-safe -b fix-auth-safe
git worktree remove ..\repo-safe
```

高级价值就一句话：

- 同一仓库里，不同尝试不要共享一个物理工作目录

## 十八、hook 什么时候值得上：直接用“漏一次就亏大了”标准
判断 hook 值不值得上，就看下面这张表。

### 值得做成 hook 的动作
- 每次提交前都要跑的最小测试
- 改到敏感目录时必须提醒的额外验证
- 长任务结束后必须生成 handoff artifact

### 不值得做成 hook 的动作
- 只是建议，不是强要求
- 触发太频繁会打断你
- 本来更适合写进 CI

### 一个可直接复用的 hook 设计清单
1. 什么时候触发
2. 触发后提示什么
3. 是提醒、阻断还是自动执行
4. 失败时允许怎么绕过
5. 这个逻辑是否更适合放 CI

## 十九、要不要接 MCP / 插件：按缺口清单决策，不要凭新鲜感
在仓库里真正实操时，先做这个判断表。

### 接入前先问 4 个问题
1. 我现在缺的是数据源、执行能力，还是自动化连接？
2. 没有这个扩展，当前任务到底卡在哪？
3. 这个扩展会不会和现有工具重叠？
4. 引入后权限边界会不会更难管？

### 一个合格的接入结论至少要写
- 用它解决什么具体问题
- 哪类任务会用
- 哪类任务不用
- 引入后的风险
- 试用观察期多久

## 二十、权限怎么分：直接按 allow / ask / deny 三层列动作
不要泛泛谈安全，直接把动作分层。

### allow
适合：
- 只读搜索
- 查看文件
- 低风险测试

### ask
适合：
- 安装依赖
- 写入多个关键文件
- 跑可能改环境的命令

### deny
适合：
- 删除大量文件
- 重置仓库状态
- 高风险外部操作

### 你可以直接让 Claude 帮你列这个表

```text
基于当前项目，把常见操作分成 allow / ask / deny 三类。
每类至少列 5 个例子，并说明为什么。
```

## 二十一、长任务具体怎么跑：用 planner / implementer / reviewer 三段式
长任务最容易烂尾的地方，不是不会分任务，而是没人负责“这一段到底算不算完成”。

最稳的三段式是：

### 1. planner
职责：

- 把模糊目标翻译成当前 sprint contract
- 划清本轮做和不做
- 指定验证方式

直接可用的提示：

```text
你先当 planner，不要写代码。
把这个需求拆成最多 3 个 sprint。
每个 sprint 都写：
1. Goal
2. Non-goals
3. Files in scope
4. Checks
5. Deliverables
6. Risks
```

### 2. implementer
职责：

- 只执行当前 sprint
- 不擅自扩 scope
- 结束时留下交接物

直接可用的提示：

```text
你现在只执行 sprint 1，不要碰 sprint 2 和 sprint 3。
严格按下面 contract 工作。
如果发现必须扩 scope，先停止并说明原因，不要自己改计划。
```

### 3. reviewer
职责：

- 不复述实现
- 只看当前 sprint 是否真的达成
- 找遗漏测试、回归风险、契约破坏

直接可用的提示：

```text
你现在是 sprint reviewer。
不要总结改动，只回答：
1. contract 里的 checks 是否真的被覆盖
2. 有没有超 scope 改动
3. 有没有缺失测试
4. 哪些风险还没关掉
```

这三段式的重点不是“角色名”，而是：

- 计划的人不直接偷跑实现
- 实现的人不擅自扩大目标
- 审查的人不被实现思路绑架

## 二十二、artifact 不是“写点总结”，而是固定交接格式
如果你只是让 Claude “总结一下刚才做了什么”，那种 artifact 价值很低。交接物必须有固定字段，不然下一轮还是得重新问。

我建议最少固定成这 7 项：

1. `Problem`
2. `Confirmed facts`
3. `Files touched`
4. `Checks passed`
5. `Checks not run`
6. `Open risks`
7. `Next best action`

直接可用的交接提示：

```text
基于刚才这轮工作，输出一份 handoff artifact，严格使用下面 7 个字段：
1. Problem
2. Confirmed facts
3. Files touched
4. Checks passed
5. Checks not run
6. Open risks
7. Next best action

要求：
- 不写空话
- 不复述无关过程
- 让我下一轮开新 session 时，直接能接着做
```

### 一个合格 artifact 的样子

```text
Problem:
session 超时后重新登录失败，表现为 refresh token 已更新但前端状态未同步。

Confirmed facts:
- 后端 refresh 接口返回正常
- `src/auth/store.ts` 没有在 refresh 成功后触发状态刷新
- 相关失败测试已能稳定复现

Files touched:
- src/auth/store.ts
- src/auth/session.ts
- tests/auth/session-refresh.test.ts

Checks passed:
- auth 相关测试通过
- refresh 场景回归测试通过

Checks not run:
- 全量 e2e 未跑

Open risks:
- 旧移动端入口是否复用同一 store 还未确认

Next best action:
- 检查移动端入口是否绕开了当前 store，并决定是否补一条兼容测试
```

这才是能交接、能恢复、能给 reviewer 用的 artifact。

## 二十三、什么时候先建 checkpoint：直接用这张触发表
下面 5 种情况，先建恢复点再动手。

1. 大规模重构前
2. 数据迁移前
3. 多文件高风险修改前
4. 批量自动修改前
5. 准备同时跑两个实验方向前

### 最低限度的做法
- 先确认当前工作区干净到什么程度
- 明确恢复点对应的范围
- 让 Claude 在开始前先输出“失败时回到哪里”

直接可用提示：

```text
开始前先做风险准备。
请先告诉我：
1. 这次改动的回退边界是什么
2. 哪些文件一旦改坏最难恢复
3. 哪一步最值得先建立 checkpoint
```

## 二十四、排障时怎么让它整理证据，而不是乱猜
直接把排障输出锁成下面这个格式。

```text
现在做排障，不要急着修。
输出格式固定为：
1. Symptom
2. Reproduction
3. Confirmed facts
4. Hypotheses
5. Evidence for each hypothesis
6. Next diagnostic step
```

### 如果你手里有日志

```text
基于下面日志，不要直接下结论。
先提取：
1. 关键错误模式
2. 时间顺序
3. 可疑模块
4. 还缺哪些日志或配置
```

## 二十五、不同入口怎么选：按任务动作选，不按偏好选
如果你要实操判断，就按下面这张表。

### Terminal
用在：
- 真正改代码
- 跑命令
- 跑测试
- 管 worktree

### IDE
用在：
- 看局部代码
- 小范围连续修改
- 边读边改

### Cloud / Web
用在：
- 远程触发任务
- PR 级别处理
- 不在本机的执行流

### Slack / Desktop
用在：
- 通知
- 轻量触发
- 协调，不是主战场

一句话用法：
- 要改代码，用 Terminal / IDE
- 要挂接远程流程，用 Cloud / Web
- 要收通知和转发任务，用 Slack / Desktop

## 二十六、把 Claude 放进 PR / CI 时，最少要让它输出这 5 项
不管是自动修复还是 PR 助手，输出至少要固定成下面 5 项：

1. Trigger
2. Files changed
3. Checks run
4. Open risks
5. Merge recommendation

直接可用提示：

```text
把这次自动修复结果整理成 PR 可审查格式，只输出：
1. Trigger
2. Files changed
3. Checks run
4. Open risks
5. Merge recommendation
```

## 二十七、给自己做一份最小作战手册，就放这 6 样东西
这一节不讲理念，直接列清单。你自己的手册至少放：

1. 一个任务壳模板
2. 一个 bugfix 模板
3. 一个 review 模板
4. 一个 sprint contract 模板
5. 一个 handoff artifact 模板
6. 一个任务结束资产分类模板

如果这 6 样东西没有固定下来，你每次都会重新组织一次工作方式。

## 二十八、团队里怎么落地：先统一这 5 个东西，再谈规模化
团队先统一这 5 件事：

1. 项目级 `CLAUDE.md` 写法
2. allow / ask / deny 权限表
3. 统一 review 输出格式
4. 统一 handoff artifact 格式
5. 哪些检查进 hook，哪些进 CI

如果团队连这 5 件事都没统一，就不要急着堆更多自动化。

## 二十九、任务结束后，直接跑一次“资产分类提示”
不要靠脑子记，直接让 Claude 分类。

```text
基于这次任务，做一次资产分类。
固定输出 5 类：
1. 该进 `CLAUDE.md` 的内容
2. 该进 memory 的内容
3. 该做成 skill 的流程
4. 该做成 hook / CI 的检查
5. 该写进团队文档或 PR 模板的内容

每类都给：
- 内容
- 放这里的理由
- 一条示例写法
```

## 三十、把整篇压成一个真实工作流，只保留可执行步骤
如果你以后只想记一套流程，就记下面这 8 步：

1. 用仓库地图模板拿整体结构
2. 用任务壳模板定义目标、范围、约束、验证、交付
3. 模糊任务先跑 plan，检查输出是否有方案、文件范围、验证清单
4. 调查链路时固定用“证据链”格式
5. 实现后必须跑 review 模板
6. 长任务必须写 sprint contract 和 handoff artifact
7. 并行或实验任务用 worktree 隔离
8. 结束后跑一次资产分类提示

如果一篇总结文档最后不能压成这类动作清单，那前面写再多也只是废话。

## 最后的场景速查表
只保留一行实操，不讲空话。

### 场景 1：刚接手新仓库
- 连跑 3 轮：仓库地图、任务链路、改动影响面

### 场景 2：需求不清
- 先让它输出方案对比表，再选方案

### 场景 3：追复杂链路
- 用固定字段：Entry points / Main call path / State changes / Related tests

### 场景 4：修 bug
- 固定走：复现 -> 证据 -> 修复 -> 回归

### 场景 5：做重构
- 先写“行为不变、契约不变、不引新依赖”

### 场景 6：补测试
- 先让它列测试场景表，再让它写测试

### 场景 7：要审查
- 切 reviewer 模板，只输出 findings 和缺失验证

### 场景 8：长任务
- 先写 sprint contract，再执行

### 场景 9：需要交接
- 固定输出 7 字段 handoff artifact

### 场景 10：并行或实验
- 直接开 worktree，不要共用一个工作目录

### 场景 11：高频且漏一次代价很大
- 评估做 hook 还是放 CI

### 场景 12：任务刚结束
- 跑一次资产分类提示

## 这篇文章最后保留的标准
以后你判断一段内容该不该留在这篇文章里，就看一条：

- 这段话能不能直接变成 prompt、命令、检查项、输出格式或交接模板

如果不能，就删。
