# 30. Claude Code 全场景实战总图：30 章总结、进阶用法与高级功能地图

## 重要程度
**S+ 级**。这不是第 1 到 29 章的压缩版目录，也不是基础功能回顾。

这篇文章要解决的是更实际的问题：

- 真实开发里，遇到一个场景，你该先开什么模式
- Claude Code 的高级能力在什么边界下最值得用
- 规则到底写进 `CLAUDE.md`、memory、skill、hook、settings 的哪一层
- 高级功能到底什么时候能提效，什么时候反而会让结果变差
- 你怎样把 Claude Code 从“偶尔帮你写几段代码”用成“稳定的开发系统”

如果你已经用了一个月 Claude Code，这一章应该成为你的总地图。

## 这篇文章怎么读
这 30 章不是按“功能说明书”写的，而是按真实开发工作顺序写的。

你可以这样用：

- 刚接新仓库：看第 1 到 6 章
- 要做需求、修 bug、重构、补测试：看第 7 到 14 章
- 要进入并行、多代理、自动化：看第 15 到 22 章
- 要做长任务、团队协作、系统化沉淀：看第 23 到 30 章

## 先给结论：真正的高手差距，主要来自这 6 件事
不是更多命令，不是更长 prompt，也不是装更多扩展。

真正能稳定拉开差距的，通常是：

1. 会先定义任务壳，而不是直接让 Claude 开写
2. 会把问题表达成可执行边界，而不是模糊地下指令
3. 会把实现、验证、审查拆开，而不是混成一锅
4. 会把规则、流程、强制检查分层沉淀，而不是全塞进一个地方
5. 会设计恢复点、交接物和工作区隔离，而不是任务乱了再补救
6. 会把 Claude Code 接入项目系统，而不是只把它当聊天窗口

## 一、把 Claude Code 的定位改对：它是执行系统，不是高级聊天框
很多人用了一个月以后，瓶颈还在，是因为定位没改过来。

你如果还在这样用：

- 想到什么就说什么
- 每轮都从头描述一遍
- 做完任务就结束，不沉淀规则
- 让 Claude 一边写一边自证自己没问题

那你用到的仍然只是低配模式。

更成熟的定位是：

- 你负责目标、边界、优先级、验收
- Claude 负责探索、实现、验证、修正、整理产出
- 项目系统负责记忆、约束、自动检查和恢复

真正高效，不是“让模型更聪明”，而是“让系统更不依赖临场发挥”。

## 二、先判断任务类型，再决定 Claude Code 的工作形态
很多失败不是因为模型不行，而是因为任务形态和工作模式不匹配。

这里说的“任务类型”，是为了帮助你选择能力，不是在规定你个人该怎么切任务。

先把任务分成 5 类：

### 1. 明确小任务
特点：
- 改动范围小
- 目标清晰
- 验证方式明确

做法：
- 直接做
- 但要提前写清验证命令或验证标准

### 2. 模糊需求任务
特点：
- 你自己也说不清到底怎么做
- 可能涉及多模块设计

做法：
- 先用 plan / 先分析后实施
- 先出方案和风险，再动代码

### 3. 调查型任务
特点：
- 重点不是写，而是查
- 需要大量读文件、追链路、看日志

做法：
- 用探索式提示
- 必要时拆给 subagent
- 主会话保留最终判断

### 4. 高风险任务
特点：
- 涉及认证、支付、迁移、权限、删除、基础设施

做法：
- 明确边界
- 明确禁改区
- 明确验证、回滚、审查

### 5. 长任务
特点：
- 无法一轮做完
- 需要多阶段交付

做法：
- 分 sprint
- 分角色
- 分工作区
- 分恢复点

很多时候你不缺提示词，你缺的是先给任务归类。

## 三、进入一个仓库后的前 30 分钟，不要急着实现
高手进入陌生仓库时，第一反应不是“帮我做 XXX”，而是先建立地图。

更稳的顺序通常是：

1. 先看目录结构、入口点、核心模块
2. 再看项目约束、命令、测试方式、风险区
3. 再看当前任务相关链路
4. 最后才开始改

你应该主动让 Claude 做这几件事：

- 讲清目录与职责分层
- 识别高风险目录
- 找出核心命令
- 说明测试入口
- 标出可能的耦合点

如果一开始就让它直接写，Claude 很可能在错误的架构假设上做出一堆“看起来能跑”的改动。

## 四、先给任务壳，不要直接给实现命令
高级用法里最值钱的动作之一，就是先给“任务壳”。

一个合格的任务壳至少应包含：

- 目标：要达成什么业务结果
- 范围：允许查看和改动哪些目录
- 约束：保持什么模式不变，不能做什么
- 验证：跑什么测试、构建、日志、截图或人工验收
- 交付：最后需要什么形式的结果

例如你不该只说：

```text
帮我修一下登录问题
```

更好的说法是：

```text
修复 session 超时后重新登录失败的问题。
重点看 auth 和 session 相关链路，先复现根因，再修复。
不要通过吞错掩盖问题，保持现有接口契约不变。
完成后跑相关测试，并总结根因、改动点和潜在回归风险。
```

高手不是更会“聊天”，而是更会定义执行边界。

## 五、上下文这件事，重点不是长度技巧，而是信息质量
如果是个人开发，什么时候重开、什么时候压缩、上下文留多长，本质上都属于个人工作习惯。

这一章真正值得记录的，不是你的手感阈值，而是 Claude Code 在上下文上的几个稳定事实：

- 当前任务相关信息，比“更多历史”更重要
- 事实、推断、已验证结论，最好分开表达
- 过时尝试和错误假设会持续影响后续输出
- 实现、审查、排障这几类任务混在一起时，更容易降低判断质量

所以这里讨论的不是“统一的上下文管理方法”，而是高质量输入应具备什么特征。

## 六、把知识放对层：`CLAUDE.md`、memory、skills、hooks、settings 各管什么
这是整个项目里最容易混乱、也最值得反复复习的一组分层。

### 1. `CLAUDE.md`
适合放：

- 项目长期事实
- 高频且稳定的规则
- 几乎每次都值得知道的约束

不适合放：

- 临时任务细节
- 一次性操作手册
- 长篇提示词

### 2. memory
适合放：

- 纠偏经验
- 工作偏好
- 容易反复犯错的提醒

不适合放：

- 项目正式规范
- 强制规则

### 3. skills
适合放：

- 可复用流程
- 固定套路
- 多步操作模板

不适合放：

- 项目事实仓库
- 大而全万能提示词

### 4. hooks
适合放：

- 不能靠自觉的动作
- 必须自动提醒或自动拦截的动作

例如：

- 提交前检查
- 改敏感目录时提醒
- 任务结束自动通知

### 5. settings / permissions / sandbox
适合放：

- 真正的能力边界
- 工具权限
- 安全策略

一句话总记忆：

- 事实进 `CLAUDE.md`
- 经验进 memory
- 流程进 skills
- 强制进 hooks
- 边界进 settings

## 七、复杂任务先 plan，不要把“边想边改”当高手风格
很多人误以为高手上来就能直接改。

其实成熟做法刚好相反：

- 小任务直做
- 多文件、多阶段、多风险任务先 plan

先 plan 的价值不只是“更稳”，而是：

- 能提前暴露你自己都没想清的地方
- 能让 Claude 在写之前先对齐风险和依赖
- 能分离“探索”和“实施”两种不同工作

你真正该学会的不是“永远先计划”，而是判断什么时候值得先计划：

- 需求不清
- 目录复杂
- 影响面大
- 要求兼容性
- 你想减少误改

如果你对方案没有把握，却还让 Claude 直接开工，失败概率通常不是线性增加，而是成倍增加。

## 八、session 的知识重点，是理解它的边界，不是规定你的习惯
这一部分不讨论你个人该多久重开一次，因为那确实是个人体验问题。

真正值得记录的知识只有 3 条：

- session 是当前工作容器，不是永久正确上下文
- session 越长，不代表判断一定越准
- 当任务性质明显变化时，继续沿用旧上下文可能会继承旧假设

至于你是偏好长 session、短 session，还是固定节奏重开，那是你的工作法，不是这篇文章要替你规定的内容。

## 九、`clear`、`compact`、restart、new session 的知识价值，在于机制区别
这部分也不提供“你该怎么用”的统一习惯，只说明这些机制本身分别解决什么问题。

### `clear`
主要作用：

- 清屏
- 改善当前可见性

它不是重置任务理解的核心机制。

### `compact`
主要作用：

- 压缩上下文
- 保留当前任务的核心状态

### restart / new session
主要作用：

- 用新的对话容器重新开始
- 摆脱旧上下文里已有的错误假设或噪音

### worktree + new session
主要作用：

- 连工作目录和上下文容器一起隔离
- 支持并行尝试、分支实验或审查分离

这里的重点是区分工具边界，而不是输出一个个人习惯模板。

## 十、理解代码时，先建地图，再追链路，再谈改动
真实开发里，“不知道代码在哪”和“知道文件但不知道怎么串”是两类不同问题。

Claude Code 在理解代码时，最好按 3 层走：

### 第一层：模块地图
要找的是：

- 入口
- 核心目录
- 数据流向
- 依赖边界

### 第二层：执行链路
要追的是：

- 用户动作从哪里进入
- 中间经过哪些函数、服务、状态或接口
- 最后落到哪里

### 第三层：改动影响面
要判断的是：

- 这次改动会碰到哪些共享组件
- 哪些地方会被顺带影响
- 哪些测试应补

如果你直接问“帮我改登录”，Claude 容易直接给你一个局部修法。
如果你先让它画出链路，再问“根因可能在哪”，结果会稳很多。

## 十一、找 bug 时，先复现和限界，不要一上来就修
高质量 bug 修复，节奏应该是：

1. 明确症状
2. 明确复现方式
3. 缩小可疑范围
4. 说明根因假设
5. 写失败用例或构造验证
6. 再修
7. 再验证

你要特别防这几种 Claude 常见误用：

- 没复现就开始补丁式乱改
- 用吞错或默认值掩盖问题
- 一次同时改三四个可疑点，导致无法归因
- 只看静态代码，不看日志、异常、测试输出

高级 bug 场景里，Claude 最强的地方不是“聪明”，而是它能在大量文本里快速归并线索。

前提是你得先把“证据链优先”写进任务要求里。

## 十二、重构时，不要奖励“看起来更现代”的改写
Claude 很会把重构做成“重写”，这在高级项目里往往是灾难。

成熟的重构指令应该强调：

- 行为不变
- 契约不变
- 小步进行
- 每步可验证
- 沿用现有风格和依赖

尤其在这些场景里要加倍克制：

- 老系统
- 共享模块
- 类型边界复杂
- 团队协作项目
- 尚未有足够测试保护的代码

如果你不给“保持行为不变”这个约束，Claude 有时会顺手把架构、命名、依赖一起重写。

那不是重构，那是借机重做。

## 十三、补测试时，Claude 最适合做的是“把隐性边界显性化”
很多人让 Claude 写测试，结果写出一堆对实现细节强绑定的脆弱测试。

更成熟的做法是让它先回答：

- 这个功能真正的输入输出边界是什么
- 哪些是回归风险最大的路径
- 哪些异常分支过去没有被表达出来

补测试最值得优先覆盖的通常是：

- 复现过的 bug
- 边界条件
- 状态切换
- 兼容性约束
- 权限和错误处理

你可以直接要求：

```text
先列出最值得补的回归场景，再补最小但有效的测试，不要为了覆盖率制造低价值断言。
```

测试不是配角。它是 Claude 改代码时最重要的护栏之一。

## 十四、把实现和 review 分开，这是高级使用的分水岭
让 Claude 既当作者又当审稿人，不是不行，但通常不够严格。

更成熟的做法是分成两轮：

### 第一轮：实现
目标：

- 达成需求
- 跑通验证
- 记录风险点

### 第二轮：review
目标：

- 站在审查视角找问题
- 优先找 bug、回归、遗漏测试、契约风险
- 不被第一轮实现思路绑住

review 提示词也应该和实现完全不同，重点改成：

- 找问题，不是复述改动
- 讲严重性
- 讲触发条件
- 讲影响范围
- 讲缺少哪些验证

很多时候，真正让产出质量上一个台阶的，不是第一次实现更聪明，而是第二轮审查更独立。

## 十五、subagent 的价值，不是“更多 AI”，而是隔离污染
子代理真正高级的用法，不是为了热闹，而是为了隔离。

最适合委派出去的任务通常有 4 类：

1. 大量读文件的调查
2. 输出很多的分析
3. 明显角色化的任务
4. 权限应受限的任务

例如：

- 让一个 agent 只查认证链路
- 让一个 agent 只跑测试和整理失败摘要
- 让一个 agent 只做安全审查
- 让一个 agent 只做文档提炼

不该委派的场景也很明确：

- 当前就需要立即拍板的核心判断
- 强依赖全局上下文的一体化设计
- 你自己还没想清任务边界

委派的原则不是“能外包就外包”，而是“把高噪音部分剥离出去”。

## 十六、多代理协作要按角色拆，不要按幻想拆
真正靠谱的多代理，通常不是一堆“超强专家”互相聊天，而是职责明确的流水线。

常见高质量分工是：

- planner：定义阶段目标、边界、交付物
- explorer：搜集证据、查链路、整理上下文
- implementer：改代码、补测试、执行验证
- reviewer：从独立视角找问题
- release / ops helper：整理 PR、CI、变更说明

多代理一旦失败，常见原因通常不是模型不行，而是：

- 角色边界不清
- 输出格式不统一
- 没有 handoff artifact
- 谁负责最终拍板不明确

多代理不是“大家一起想”，而是“每个人只负责一个清晰环节”。

## 十七、worktree 是并行开发和风险隔离的默认搭档
如果你已经开始做高级任务，却还只有一个工作目录在来回切换，效率很容易撞墙。

worktree 特别适合这些场景：

- 两个需求并行推进
- 一个分支实现，一个分支 review
- 一个分支稳定修复，一个分支做激进实验
- 长任务需要阶段隔离

它的价值不只是并行，更在于：

- 不互相覆盖文件
- 不互相污染自动记忆和局部上下文
- 更容易回溯每条尝试
- 更适合把 agent 分配到独立空间

worktree 的高级意义，不在 git 技巧，而在“给不同类型的思考容器物理隔离”。

## 十八、hooks 不是锦上添花，而是把人从低价值盯梢里解放出来
只要一件事满足下面两个条件，就值得考虑 hook：

- 每次都该做
- 忘了代价很大

典型适用项：

- 提交前跑关键检查
- 修改敏感目录时提醒额外验证
- 长任务结束后发通知
- 某些命令前后自动附加上下文动作

但 hook 也不是越多越好。

低质量 hook 的问题通常是：

- 触发太频繁，形成噪音
- 规则太模糊，用户最终无视
- 做了本该写进流程模板的事
- 做了本该由 CI 保证的事

最好的 hook 应该短、硬、少、关键。

## 十九、MCP、插件与扩展体系，先解决明确缺口，再谈生态热闹
很多人中级以后最容易掉进一个坑：

**功能越多越强。**

这在 Claude Code 里通常不成立。

你接 MCP 或插件之前，先问自己：

- 我缺的是数据源、能力还是自动化连接？
- 这个扩展是高频刚需，还是低频噪音？
- 它会不会让工具选择变复杂？
- 它会不会让权限边界变模糊？

更成熟的扩展策略通常是：

1. 默认保持最小工具集
2. 遇到确定缺口再引入
3. 新扩展先小范围试用
4. 有价值再沉淀为稳定工作流

生态不是收集癖。真正的高手会主动减少工具重叠。

## 二十、权限与沙箱不是烦人的限制，而是让自动化可持续的前提
高级用户真正该追求的，不是“怎样少弹确认”，而是“怎样让系统能安全自动跑”。

权限设计里要分清：

- 哪些操作可以默认放行
- 哪些必须询问
- 哪些应直接禁止

好的权限边界带来的收益是：

- 风险任务不会误放大
- 低风险任务可以自动推进
- 团队协作时更容易治理
- 回溯时能看清谁在什么边界内行动

如果你为了追求顺滑，把所有权限都开到最大，短期也许舒服，长期通常会让你失去自动化信心。

真正高级的目标不是“无阻力”，而是“低摩擦但有护栏”。

## 二十一、长任务场景下，阶段契约比个人分任务习惯更重要
个人开发里怎么切任务，确实常常带有强烈个人风格。

但一旦进入长任务、多人协作、自动化执行或跨 session 接力，真正重要的就不再是“你平时怎么切”，而是有没有阶段契约。

这里更值得记录的是：

- 当前阶段做什么
- 当前阶段不做什么
- 这一段怎么验收
- 失败后回到哪里
- 下一轮接力需要什么输入

这不是在规定个人习惯，而是在强调长任务一旦跨阶段，就需要可交接、可验证的完成定义。

## 二十二、交接靠 artifact，不靠“你应该还记得”
只要任务跨轮次、跨 session、跨 agent、跨工作区，就应该留下交接物。

高价值 artifact 可以是：

- 当前问题定义
- 已确认的根因
- 已尝试但放弃的路线
- 相关文件列表
- 当前验证结果
- 剩余风险
- 下一步建议

很多高级用户会卡住，并不是因为不会操作，而是任务一旦跨容器，就缺少稳定交接物。

只要交接物写得好：

- 新 session 起得快
- review 更独立
- 多代理更稳
- 长任务更能恢复

交接文件不是文档负担，它是高杠杆压缩包。

## 二十三、Checkpointing 的意义，不只是回滚，而是允许大胆试验
很多人只在做坏了时才想到恢复点。

更成熟的理解是：

- 恢复点不是给失败准备的羞耻按钮
- 它是让你敢于做中高风险试验的前提设施

你该在这些时刻主动建立恢复点：

- 大规模重构前
- 数据迁移前
- 多文件高风险改动前
- 自动化批量修改前
- 准备切分多个实验方向前

恢复能力越强，你越能把 Claude 用到更高杠杆的场景。

没有 checkpoint 的自动化，通常会让人下意识地只敢让 Claude 做低风险杂活。

## 二十四、调试和排障时，要把“观察系统”搭起来
高级排障不是靠灵感，而是靠观测。

你应该主动让 Claude 帮你整理这些东西：

- 错误日志的关键模式
- 异常触发前后的调用链
- 配置差异
- 环境变量影响
- 时间相关问题
- 并发和状态问题

Claude 在排障里很强的一点，是它能快速把碎片线索变成假设清单。

但你必须要求它：

- 区分已证实和未证实
- 给出下一步验证动作
- 不要拿猜测代替证据

调试最怕的不是没思路，而是让错误假设在上下文里活太久。

## 二十五、IDE、Terminal、Cloud、Web、Slack 等入口，各自适合不同阶段
高级使用里，一个很重要的能力是“场景选入口”。

### Terminal / 本地仓库
最适合：

- 真实改代码
- 跑命令
- 做验证
- 进入项目主流程

### IDE 集成
最适合：

- 代码浏览
- 局部改动
- 编辑器内连续协作

### Cloud / Web / PR 修复
最适合：

- 远程任务
- 代码托管平台事件触发
- 非本机执行链路

### Slack / Desktop / 其他平台扩展
最适合：

- 通知
- 协调
- 轻量触发
- 任务入口转发

高阶思维不是“一个入口干所有事”，而是“让不同入口各自承担最合适的那一段”。

## 二十六、GitHub、CI/CD 与自动修复，重点不是自动，而是可审计
当 Claude 开始进入 PR、CI、自动修复链路后，标准会变化。

这时最重要的不是“自动完成更多”，而是：

- 谁触发的
- 改了什么
- 怎么验证的
- 哪些风险仍未覆盖
- 谁审批上线

自动修复真正可用的前提通常是：

- 任务边界窄
- 验证步骤明确
- 输出格式可审查
- 失败后能退出并留痕

CI/CD 里的 Claude 不该像一个黑箱魔法，而应该像一个有边界、有记录、有恢复能力的执行节点。

## 二十七、个人开发者真正该建立的是“最小高杠杆作战手册”
你用了一个月 Claude Code 后，最值得做的不是继续找更多技巧，而是收束成你自己的最小系统。

这份最小系统至少应包含：

- 你的任务起手模板
- 你的 review 模板
- 你的 bugfix 模板
- 你的文档沉淀规则
- 你的高风险操作清单
- 你的任务结束资产分类清单

真正的高手不会每次临场发挥。

他们会把高频决策提前固化，所以每次做任务时，脑力用在业务判断，不用在重复组织工作方式上。

## 二十八、团队协作真正难的不是功能不够，而是治理分层不清
个人能跑起来，不代表团队能稳定跑。

团队层面的关键问题通常是：

- 什么进项目规则，什么留给个人偏好
- 哪些权限默认开，哪些必须审批
- 哪些流程自动化，哪些保留人工 review
- 怎样让不同成员产出质量接近
- 怎样审计 Claude 的参与方式

团队环境里最该避免的是：

- 大家各写一套野生提示词
- 配置、记忆、技能、hook 乱放
- 自动化越加越多，却没人维护
- 结果出了问题，没人说得清责任边界

团队要的不是“每个人都很会用”，而是“整体系统可复制、可治理、可恢复”。

## 二十九、最高杠杆动作：每次任务结束都做一次资产分类
这是整个高级体系里最容易被忽视，但长期回报最高的动作。

任务做完后，不要只看“这个 bug 修好了没有”，还要问：

### 哪些内容该进入 `CLAUDE.md`
例如：

- 长期事实
- 项目固定约束
- 关键命令

### 哪些内容该进入 memory
例如：

- 这类任务的纠偏经验
- 特别容易犯错的点

### 哪些内容该沉淀成 skill
例如：

- 已经重复出现的多步流程

### 哪些内容该变成 hook
例如：

- 不能靠自觉记住的关键检查

### 哪些内容该进入团队文档或 PR 模板
例如：

- 可审查的交付规范
- 统一的风险说明格式

绝大多数人只用 Claude 完成当前任务。
高手会让当前任务变成以后任务的加速器。

## 三十、最后的硬结论：高级用法的本质，是把 Claude Code 变成你的项目操作系统
如果把前面 29 章再压成一句话，那就是：

**Claude Code 最强的地方，不是某个单点功能，而是它可以成为项目里的执行层、整理层、验证层和协作层。**

真正成熟的使用方式，通常长这样：

1. 先定义任务壳和验收
2. 再选择合适的能力和执行边界
3. 必要时先 plan，再实施
4. 把调查、实现、review、自动化分层
5. 给每个高风险动作配验证和恢复点
6. 用 memory、skills、hooks、worktree、subagent 把经验系统化
7. 每次任务结束都做资产分类

最终你要达到的状态不是：

- 会很多命令
- 知道很多小技巧
- 装了很多扩展

而是：

- 新仓库能快速建图
- 模糊需求能稳定拆解
- 高风险任务能先设护栏再执行
- 长任务能分阶段推进和恢复
- review 能独立于实现
- 经验能不断沉淀成项目资产

这时 Claude Code 才不再只是“帮你写代码”，而是真正进入你的真实开发系统。

## 最后的场景速查表
如果你以后只记这份速查表，也够用了。

### 场景 1：刚接手新仓库
- 先看整体地图、入口、约束、风险区
- 不要直接实现

### 场景 2：需求不清
- 先 plan
- 先写完成定义

### 场景 3：要追一条复杂链路
- 先找文件
- 再追流
- 再判断改动点

### 场景 4：修 bug
- 先复现
- 先找证据
- 先限界
- 再修

### 场景 5：做重构
- 明写行为不变
- 小步验证

### 场景 6：补测试
- 优先补回归和边界
- 不写低价值断言

### 场景 7：结果总跑偏
- 先检查是不是目标、约束、验证条件表达得不够清楚
- 再决定要不要换容器或换执行方式

### 场景 8：任务很长
- 长任务要有阶段验收
- 留 artifact
- 设 checkpoint

### 场景 9：输出很多、读文件很多
- 考虑 subagent
- 主会话做综合判断

### 场景 10：并行需求或实验
- 用 worktree 隔离
- 不要在同一工作区搅在一起

### 场景 11：每次都容易忘关键动作
- 不要靠意志力
- 直接考虑 hook 或 CI

### 场景 12：任务刚做完
- 立即做资产分类
- 想清楚哪些该进入系统

## 这篇文章真正想让你形成的习惯
不是“以后怎么写更厉害的 prompt”。

而是：

- 先定义任务，再调用能力
- 先提高输入质量，再谈上下文技巧
- 先设计验证，再放手执行
- 先沉淀系统，再追求速度

当你这样使用 Claude Code 时，你已经不再处于“会用工具”的阶段，而是在进入“设计工作系统”的阶段。
